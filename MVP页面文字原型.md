# XE6-15 MVP：页面文字原型

> 状态：已完成文字评审，待转为原型  
> 画布建议：桌面 Web，单页尺寸 `1440 × 960`  
> 需求来源：Proposal01 Proposal02 Proposal03

## 1. MVP 已确认约束

1. MVP 只连接一个 GitHub Repository，不提供多仓库聚合或切换。
2. Repository 不要求是公开仓库；只要当前用户拥有访问权限或参与其中，就可以选择。
3. 未登录时，开屏直接展示“连接 GitHub”，不先进入产品首页或设置页。
4. 已登录且已绑定 Repository 的用户打开产品时，默认进入“今日计划”；“全部待办”作为次级入口保留。
5. 连接后自动同步与当前用户相关的开放 Issue、待处理 PR Review 和 Milestone 信息，不要求用户手动开启同步。
6. GitHub 的事实字段与产品侧规划字段分开：
   - GitHub 事实：标题、状态、Assignee、Reviewer、Milestone、Repository、原始链接；
   - 产品规划：P0/P1/P2、硬截止、预计投入、计划日期、手动顺序。
7. 系统生成预计投入建议时，必须明确对应到具体任务，供用户逐项接受或修改。
8. 内部排序顺序采用：`24 小时内到期置顶 → P0/P1/P2 → DDL → 预计投入 → 老化上浮`。
9. 产品不向用户展示完整排序规则，只在任务旁展示与该任务直接相关的简短原因，例如“24 小时内到期”“P0”“8 天未活动”。
10. 自动重排直接生效；只有硬截止任务已超过可用容量、无法自动形成可执行计划时，才要求用户选择处理方式。

## 2. 页面总览与主流程

| 编号 | 页面 | 用户目标 | 主要去向 |
|---|---|---|---|
| 01 | 连接 GitHub | 完成身份授权 | 02 选择单一仓库 |
| 02 | 选择单一仓库 | 绑定一个有权访问的仓库 | 03 同步与时间建议 |
| 03 | 同步与时间建议 | 查看导入结果，处理缺失的预计投入 | 06 今日计划 |
| 04 | 全部待办 | 查看个人任务池及当前优先顺序 | 05 任务详情 / 06 今日计划 |
| 05 | 任务详情 | 区分 GitHub 事实与个人规划并修改规划字段 | 返回 04 或进入 GitHub |
| 06 | 今日计划 | 按今日容量执行已经排好的任务 | 05 任务详情 / 07 自动重排 |
| 07 | 自动重排 | 理解哪些任务发生了变化以及具体原因 | 06 今日计划 / 04 全部待办 |
| 08 | 硬截止容量冲突 | 在系统无法自动解决时选择处理方向 | 更新后的 06 今日计划 |

```text
01 连接 GitHub
      ↓
02 选择单一仓库
      ↓
03 同步与时间建议
      ↓
06 今日计划
      ├─ 查看全部任务 → 04 全部待办 ─→ 05 任务详情
      ├─ 查看今日任务 ────────────→ 05 任务详情
      ↓ 任务状态、截止时间或日期发生变化
07 自动重排
      ├─ 可形成计划 → 返回 06 今日计划
      └─ 硬截止任务超出容量 → 08 硬截止容量冲突
```

## 3. 页面 01：连接 GitHub

### 页面目标

让首次打开产品、尚未登录的用户直接完成 GitHub 授权，避免先进入一个没有数据的空工作台。

### 文字线框

```text
┌──────────────────────────────────────────────────────────────┐
│ XE6-15                                                       │
│                                                              │
│                    把 GitHub 任务变成                         │
│                    今天可执行的计划                            │
│                                                              │
│        连接一个你参与的 GitHub 仓库，自动汇总与你有关的            │
│        Issue 和 PR Review，并形成个人任务计划。                  │
│                                                              │
│                    [ 连接 GitHub ]                           │
│                                                              │
│        将申请读取身份、仓库及 Issue / PR 基础信息。               │
│        产品不会在 GitHub 中替你创建或关闭任务。                   │
└──────────────────────────────────────────────────────────────┘
```

### 关键交互

- 点击“连接 GitHub”进入 GitHub OAuth 授权。
- 授权成功后自动进入页面 02。
- 授权失败或取消时留在当前页，在按钮下方显示可重试的错误提示。

## 4. 页面 02：选择单一仓库

### 页面目标

从用户有权访问或参与的仓库中绑定一个 Repository，明确 MVP 的单仓库上下文。

### 文字线框

```text
┌──────────────────────────────────────────────────────────────┐
│ ← 返回                 选择工作仓库                 @Jiang      │
├──────────────────────────────────────────────────────────────┤
│ 选择一个仓库，XE6-15 将从这里同步与你有关的任务。                   │
│ 目前一次只支持一个仓库。                                         │
│                                                              │
│ 搜索仓库  [ 输入组织或仓库名称……                         ]       │
│                                                              │
│ 你的仓库                                                      │
│ ○ 1024XEngineer / XE6-15               Private · 有访问权限    │
│ ○ 1024XEngineer / course-examples      Public  · Contributor │
│ ○ Jiang / personal-lab                 Private · Owner       │
│                                                              │
│ 已选择：1024XEngineer / XE6-15                                │
│                                                              │
│                                         [ 连接此仓库 ]         │
└──────────────────────────────────────────────────────────────┘
```

### 关键交互

- 列表同时包含用户可访问的公开和私有仓库，不以是否公开作为筛选条件。
- 使用单选控件；选择新仓库会取消之前的选择。
- 未选择仓库时，“连接此仓库”不可用。
- 点击后保存唯一绑定并进入页面 03；页面中不出现“再添加一个仓库”。

## 5. 页面 03：同步与时间建议

### 页面目标

让用户确认首次自动同步结果，并逐项处理缺少预计投入的任务。

### 文字线框

```text
┌──────────────────────────────────────────────────────────────┐
│ XE6-15       1024XEngineer / XE6-15              同步完成 ✓   │
├──────────────────────────────────────────────────────────────┤
│ 已找到与你有关的任务                                            │
│                                                              │
│ 6 个开放 Issue    1 个待处理 PR Review    2 个 Milestone       │
│ GitHub 内容会自动保持同步。                                     │
│                                                              │
│ ┌─ 需要你确认预计投入 ─────────────────────────────────────┐    │
│ │ Issue #51  更新项目文档                                 │    │
│ │ 系统建议：1h             [接受 1h] [修改]                │    │
│ │                                                       │    │
│ │ PR #45  审阅任务列表交互                                 │    │
│ │ 系统建议：45min          [接受 45min] [修改]             │    │
│ └───────────────────────────────────────────────────────┘    │
│                                                              │
│ 其余 5 项已有时间信息                                           │
│                                         [ 进入今日计划 ]       │
└──────────────────────────────────────────────────────────────┘
```

### 关键交互

- 时间建议必须显示在对应任务行中，不能只显示“有 2 项建议”而不指出任务。
- “接受”只保存该任务的预计投入；“修改”切换为时间输入框。
- 用户可以暂不处理并进入页面 06；未确认的任务显示为“待估算”，暂不进入按容量生成的今日计划。
- 首次同步完成后，以及用户之后再次打开产品时，默认落在“今日计划”；“全部待办”通过左侧导航进入。
- 页面展示最近同步状态，但不提供“是否自动同步”的开关。

## 6. 页面 04：全部待办

### 页面目标

以统一任务池展示当前用户需要处理的 Issue 与 PR Review，并让用户快速理解每项任务为何处在当前位置。

### 文字线框

```text
┌──────────────┬────────────────────────────────────────────────┐
│ XE6-15       │ 全部待办                         共 7 项         │
│              ├────────────────────────────────────────────────┤
│ ○ 今日计划    │ [全部] [Issue] [PR Review]   搜索……              │
│ ● 全部待办    │                                                 │
│ ○ 待评审      │ 1  Issue #42  提交训练营作业          2h          │
│ ○ 阻塞任务    │    P0 · 今天截止                                │
│ ○ 设置        │    排序依据：24 小时内到期                        │
│              │                                                │
│ 仓库          │ 2  Issue #31  完成 GitHub 登录页       2h       │
│ 1024X…/XE6-15│    P0 · 7 月 16 日                              │
│              │    排序依据：P0                                  │
│              │                                                │
│              │ 3  PR #45  审阅任务列表交互          45min        │
│              │    P1 · 无硬截止                                │
│              │    排序依据：等待你的 Review                      │
│              │                                                │
│              │ 4  Issue #18  补充错误提示             1h        │
│              │    P2 · 无硬截止                                │
│              │    排序依据：8 天未活动                           │
│              │                                                │
│              │ 5  Issue #51  更新项目文档          待估算        │
│              │    [补充预计投入]                                │
└──────────────┴────────────────────────────────────────────────┘
```

### 展示原则

- 列表默认已排序，但页面不展示完整算法、权重、公式或“规则说明”入口。
- 每个任务最多展示一至两个当前最有用的简短原因。
- 硬截止、P0/P1/P2、预计投入和老化信息按需展示，不虚构缺失值。
- Issue 与 PR Review 使用不同类型标签，但保留在同一个个人任务池中。
- 点击任务进入页面 05；点击“今日计划”进入页面 06。

## 7. 页面 05：任务详情

### 页面目标

查看一个任务的完整上下文，并清楚区分 GitHub 事实与产品内个人规划。

### 文字线框

```text
┌──────────────┬────────────────────────────────────────────────┐
│ XE6-15       │ ← 全部待办                                      │
│              │                                                │
│ ○ 今日计划    │ Issue #31  完成 GitHub 登录页                   │
│ ● 全部待办     │ On Progress · 1024XEngineer / XE6-15           │
│ ○ 待评审      │ [ 在 GitHub 中查看 ↗ ]                          │
│ ○ 阻塞任务    │                                                │
│              │ ┌─ GitHub 信息 ─────────────────────────────┐  │
│              │ │ 状态：Open        Assignee：@Jiang        │   │
│              │ │ Milestone：MVP    最近更新：7 月 13 日     │   │
│              │ └──────────────────────────────────────────┘   │
│              │                                                │
│              │ ┌─ 我的规划 ────────────────────────────────┐   │
│              │ │ 优先级       [P0|⌄]                       │   │
│              │ │ 硬截止       [2026-07-16]                 │   │
│              │ │ 预计投入     [2h]                         │   │
│              │ │ 当前安排     今日计划                       │   │
│              │ └──────────────────────────────────────────┘   │
│              │                                                │
│              │ 当前排序依据：P0                    [保存]        │
└──────────────┴────────────────────────────────────────────────┘
```

### 关键交互

- GitHub 信息只读；用户通过外链返回 GitHub 查看或修改原始对象。
- 用户可以修改产品侧优先级、硬截止与预计投入。
- 保存规划字段后，如果顺序或今日计划受影响，系统自动重排并进入页面 07。
- 页面只解释当前任务的直接原因，不展示全局排序规则。

## 8. 页面 06：今日计划

### 页面目标

作为已登录用户的默认首页，给用户一份符合今日容量、可以直接执行的任务列表，同时保留全部待办、待评审、规划池和待估算任务的入口。

### 文字线框

```text
┌──────────────┬────────────────────────────────────────────────┐
│ XE6-15       │ 今日计划                         7 月 14 日      │
│              ├────────────────────────────────────────────────┤
│ ● 今日计划    │ 今日容量 4h     已安排 4h     剩余 0h             │
│ ○ 全部待办    │ [████████████████████████████████████] 100%    │
│ ○ 待评审      │                                                │
│ ○ 阻塞任务    │ 1  Issue #42  提交训练营作业          2h         │
│              │    今天截止 · 排序依据：24 小时内到期              │
│              │                                    [查看任务]   │
│              │                                                │
│              │ 2  Issue #31  完成 GitHub 登录页       2h       │
│              │    P0 · 7 月 16 日 · 排序依据：P0                │
│              │                                    [查看任务]   │
│              │                                                │
│              │ ─────────────────────────────────────────────  │
│              │ 待评审 1 项 · 规划池 3 项 · 待估算 1 项            │
│              │ [查看待评审] [查看规划池] [处理待估算]              │
└──────────────┴────────────────────────────────────────────────┘
```

### 关键交互

- 容量区域展示今日总容量、已安排和剩余容量。
- 同一硬截止分组内允许拖动任务，保存用户手动顺序。
- 点击任务进入页面 05；实际任务处理仍回到 GitHub。
- 新任务、状态变化、截止变化或跨日发生时自动更新，不提供“重新规划”按钮。

## 9. 页面 07：自动重排

### 页面目标

任务发生变化后，直接展示已经生效的新计划和逐项变化原因，让用户理解结果后继续工作。

### 文字线框

```text
┌──────────────────────────────────────────────────────────────┐
│ 计划已更新                                         刚刚        │
├──────────────────────────────────────────────────────────────┤
│ GitHub 中的任务状态发生变化，今日计划已自动调整。                   │
│                                                              │
│ + 加入今日计划                                                 │
│   Issue #31  完成 GitHub 登录页返工                  1h        │
│   排序依据：PR 收到 changes requested，需要你继续处理             │
│                                                              │
│ − 移回规划池                                                   │
│   Issue #18  补充错误提示                            1h        │
│   排序依据：今日容量不足，且优先级较低                             │
│                                                              │
│ ↕ 顺序变化                                                    │
│   Issue #42 已移动到第 1 位                                    │
│   排序依据：24 小时内到期                                       │
│                                                              │
│                  [查看全部待办]  [返回今日计划]                  │
└──────────────────────────────────────────────────────────────┘
```

### 展示原则

- 本页不是“排序规则”页面，不展示完整优先级链条、算法或内部计算过程。
- 只展示本次真正发生变化的任务、变化类型和直接原因。
- 用户不需要逐项确认；进入本页时新计划已经生效。
- 如果任务只是移出今日计划，必须说明它回到了规划池，并未被删除或关闭。
- 若无法自动形成计划，则从本页进入页面 08。

## 10. 页面 08：硬截止容量冲突

### 页面目标

当必须在今天完成的任务本身已超过今日容量时，明确暴露缺口并让用户决定，不静默延期。

### 文字线框

```text
┌──────────────────────────────────────────────────────────────┐
│ ⚠ 硬截止容量冲突                                               │
├──────────────────────────────────────────────────────────────┤
│ 今天必须完成的任务超过了可用时间，系统无法自动生成可执行计划。         │
│                                                              │
│ 今日剩余容量：2h       必须今日完成：3h       缺口：1h             │
│                                                              │
│ Issue #31  完成 GitHub 登录页返工                    1h        │
│ 排序依据：今天截止                                              │
│                                                              │
│ Issue #42  提交训练营作业                            2h        │
│ 排序依据：今天截止                                              │
│                                                              │
│ 请选择处理方向：                                                │
│ [调高今日容量]                                                 │
│ [调整任务范围或预计投入]                                         │
│ [手动移出一个任务]                                              │
│ [接受可能延期]                                                 │
│                                                              │
│ 在你选择前，任务截止时间不会被自动修改。                            │
└──────────────────────────────────────────────────────────────┘
```

### 关键交互

- “调高今日容量”打开容量输入；确认后重新计算今日计划。
- “调整任务范围或预计投入”进入所选任务的页面 05。
- “手动移出一个任务”要求用户明确选择任务，产品不默认替用户牺牲其中一项。
- “接受可能延期”保留冲突标识，并记录这是用户明确接受的风险。
- 任一操作产生可执行计划后返回页面 06；仍有缺口时继续留在本页。

## 11. 跨页面统一规则

### 导航与仓库上下文

- 登录和绑定完成后，左侧导航固定展示“今日计划、全部待办、待评审、阻塞任务、设置”。
- “今日计划”是已登录用户的默认首页；“全部待办”不作为默认落地页。
- 页面始终展示当前唯一 Repository；MVP 不提供仓库切换器。

### 任务类型与状态

- 类型标签：`Issue`、`PR Review`。
- 主状态：`On Progress`、`Pending Review`、`Done`。
- “阻塞”“待估算”“changes requested”“已合并”“已关闭未合并”作为原因或展示标签，不新增主状态。

### 空状态与异常状态

- 没有相关任务：显示“当前没有需要你处理的任务”，并提供“前往 GitHub”链接。
- GitHub 暂时无法同步：保留上次成功数据，显示同步失败时间和“重试同步”。
- 缺少硬截止：显示“无硬截止”，不生成虚假日期。
- 缺少预计投入：显示“待估算”，不凭空加入按容量生成的今日计划。
- Repository 权限失效：保留本地规划数据并要求重新连接 GitHub，不自动改绑其他仓库。
