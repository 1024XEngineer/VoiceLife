# 本地领域服务层：数据模型与接口文档

## 1. 服务边界

本地领域服务层位于 LLM 调度引擎（Function Calling）与操作系统适配层/SQLite 数据库之间。

它的职责是：接收结构化业务请求、执行领域校验和状态变更、保证一次业务操作的本地数据一致、生成可回看的会话回执。

它不负责语音转文字、意图识别、文本转语音、音频播放、系统通知显示和界面渲染。这些能力分别属于图中的智能会话核心层、用户交互层和操作系统适配层。

| 服务 | 核心职责 | 读写模型 |
| --- | --- | --- |
| 日历服务 | 创建、查询和读取日历事件；生成可提醒的事件实例。 | 日历事件、提醒事项、会话回执 |
| 提醒服务 | 管理提醒状态；生成、关闭或推迟提醒。 | 提醒事项、会话回执 |
| 事务撤销服务 | 保存可逆操作的变更快照并恢复指定操作。 | 撤销事务记录、相关业务模型、会话回执 |
| 日历变更服务 | 修改、取消、跳过或变更既有日历事件。 | 日历事件、提醒事项、撤销事务记录、会话回执 |
| 备忘录服务 | 记录和查询 24 小时临时便签。 | 备忘录、会话回执 |

## 2. 通用约定

```ts
type Id = string;                   // 由服务生成的全局唯一标识
type ISODateTime = string;          // 含时区的 ISO 8601 时间
type Nullable<T> = T | null;
type ConfirmationToken = string;    // 服务端签发的 opaque token，不向调用方暴露编码内容

interface ServiceContext {
  requestId: Id;                    // 本次 Function Calling 请求的幂等键
  conversationId: Id;               // 对话会话视图的关联键
  occurredAt: ISODateTime;
}

interface ServiceResult<T> {
  success: boolean;
  data?: T;
  error?: ServiceError;
  receipt?: ConversationReceipt;
}

interface ServiceError {
  code: string;                     // 机器可读的错误码
  message: string;                  // 面向调用方的说明
  retryable: boolean;
  details?: Record<string, unknown>;
}
```

所有写接口必须在一个本地数据库事务中完成“业务实体变更 + 撤销记录（如有）+ 会话回执”。`conversation_receipts.request_id` 是写请求的原子幂等键：首次请求在该事务内写入回执及其可重建结果的 `payload`；同一 `requestId` 再次写入触发唯一约束冲突时，必须回滚本次业务写入、读取已存回执并返回首次 `ServiceResult`，不能重复创建日程、提醒或回执。事务成功提交后，才允许通知操作系统适配层执行本地通知或音频播放。

冲突或高风险操作的确认令牌只能由服务端签发。它必须是 opaque 值，并绑定具体操作、规范化后的请求参数、目标与候选集（如适用）和 `requestId`；令牌有明确过期时间，且只能在成功写入的同一事务中消费一次。已成功的同一 `requestId` 重试优先返回既有结果；其他已消费、过期或绑定不匹配的令牌必须返回业务错误，不能执行写入。

## 3. 数据模型

### 3.1 日历事件 `CalendarEvent`

日历事件是日历服务的主实体，表示用户创建的一条单次或周期性安排。

```ts
type CalendarEventStatus = "active" | "cancelled" | "completed";
type EventKind = "point" | "time_block";
type RecurrenceFrequency = "daily" | "weekly" | "monthly";

interface CalendarEvent {
  id: Id;
  title: string;
  startAt: ISODateTime;
  endAt: Nullable<ISODateTime>;
  kind: EventKind;
  timeZone: string;
  location: Nullable<string>;
  description: Nullable<string>;
  recurrenceFrequency: Nullable<RecurrenceFrequency>;
  recurrenceWeekday: Nullable<number>;     // 1=周一，7=周日
  recurrenceMonthDay: Nullable<number>;    // 1..31
  reminderOffsetMinutes: number;
  weakReminderEnabled: boolean;
  weakReminderMinutes: Nullable<15>;
  pausedFrom: Nullable<ISODateTime>;
  pausedUntil: Nullable<ISODateTime>;
  terminatedAt: Nullable<ISODateTime>;
  status: CalendarEventStatus;
  createdAt: ISODateTime;
  updatedAt: ISODateTime;
}
```

| 字段 | 约束/含义 |
| --- | --- |
| `title` | 必填，用户可识别的日程名称。 |
| `startAt` | 必填，事件发生开始时间。 |
| `kind` | `point` 只在触发点提醒；`time_block` 为占用时间段。 |
| `endAt` | `time_block` 必填且必须晚于 `startAt`；`point` 为 `null`。 |
| `timeZone` | 必填，采用 IANA 时区名，例如 `Asia/Shanghai`。 |
| `recurrenceFrequency` | 仅支持 `daily`、`weekly`、`monthly`；不支持复杂 RRULE。 |
| `reminderOffsetMinutes` | 主提醒相对事件开始时间的提前分钟数；默认 `0`。 |
| `weakReminder*` | 有明确开始时间的日程可在开始前固定 15 分钟发弱提醒。 |
| `paused*` / `terminatedAt` | 仅周期事项使用，分别表达临时暂停与终止后续周期。 |
| `status` | 已取消事件不可继续产生提醒。 |

### 3.2 日程实例 `Occurrence`

`Occurrence` 是由日历事件和覆盖项解算出的可展示、可操作的一次发生实例；它是查询、跳过和“仅本次”变更的稳定目标。

```ts
interface Occurrence {
  eventId: Id;
  title: string;
  originalStartAt: ISODateTime;
  effectiveStartAt: ISODateTime;
  effectiveEndAt: Nullable<ISODateTime>;
  kind: EventKind;
  timeZone: string;
  location: Nullable<string>;
  description: Nullable<string>;
  recurrenceFrequency: Nullable<RecurrenceFrequency>;
  moved: boolean;
}
```

### 3.3 提醒事项 `ReminderItem`

提醒事项是对某个日历事件（或其一个发生实例）的本地提醒任务。

```ts
type ReminderStatus = "scheduled" | "pushed" | "snoozed" | "closed";

interface ReminderItem {
  id: Id;
  eventId: Id;
  originalStartAt: ISODateTime;
  effectiveStartAt: ISODateTime;
  triggerAt: ISODateTime;
  status: ReminderStatus;
  snoozeCount: number;              // 0..3
  voiceDeliveredAt: Nullable<ISODateTime>;
  closedAt: Nullable<ISODateTime>;
  createdAt: ISODateTime;
  updatedAt: ISODateTime;
}
```

```text
scheduled ──本地定时任务触发──> pushed ──关闭──> closed
                                │
                                └──推迟（最多 3 次）──> snoozed ──再次触发──> pushed
```

`eventId + originalStartAt` 唯一关联周期日程的特定一次发生；对单次事件，`originalStartAt` 等于事件的 `startAt`。推迟仅改本提醒的 `triggerAt`，绝不改底层日程。

### 3.4 弱提醒事项 `WeakReminderItem`

```ts
type WeakReminderStatus = "scheduled" | "delivered" | "cancelled";

interface WeakReminderItem {
  id: Id;
  eventId: Id;
  originalStartAt: ISODateTime;
  effectiveStartAt: ISODateTime;
  triggerAt: ISODateTime;            // effectiveStartAt - 15 分钟
  status: WeakReminderStatus;
  deliveredAt: Nullable<ISODateTime>;
  createdAt: ISODateTime;
  updatedAt: ISODateTime;
}
```

弱提醒只是提前轻提示，不要求用户响应，不替代主提醒，也不能被关闭或推迟。

### 3.5 配置覆盖项 `OccurrenceOverride`

功能清单与 Issue #53 已确认它用于保存周期日程某一次实例的改期、字段覆写或跳过，避免直接破坏原系列规则。

```ts
type OverrideStatus = "moved" | "skipped";

interface OccurrenceOverride {
  id: Id;
  eventId: Id;
  originalStartAt: ISODateTime;
  newStartAt: Nullable<ISODateTime>;
  patch: Partial<Pick<CalendarEvent, "title" | "endAt" | "timeZone" | "location" | "description">>;
  status: OverrideStatus;
  createdAt: ISODateTime;
  updatedAt: ISODateTime;
}
```

`newStartAt = null` 且 `status = skipped` 表示跳过该实例。周期范围“仅本次 / 本次及以后 / 整个系列”属于日历变更服务的命令语义：仅本次写此表；本次及以后通过拆分系列实现；整个系列直接更新事件。

### 3.6 备忘录 `Memo`

```ts
interface Memo {
  id: Id;
  content: string;
  category: Nullable<string>;
  createdAt: ISODateTime;
  expiresAt: ISODateTime;            // 创建后固定 24 小时
}
```

备忘录用于没有明确日程时间的自由文本记录。若用户表达了明确的提醒时间，LLM 应调用日历服务创建日程，而不是写入备忘录。

### 3.7 提醒偏好 `ReminderPreference`

提醒偏好由图中的“应用配置面板”维护，提醒服务只读取生效值；它不是日历事件或提醒实例的一部分。

```ts
interface ReminderPreference {
  defaultSnoozeMinutes: number;     // 首次默认 10，取值为 1..1440 的整数
  updatedAt: ISODateTime;
}

interface ReminderPreferenceProvider {
  get(): ReminderPreference;
  set(input: { defaultSnoozeMinutes: number }): ReminderPreference;
}
```

“稍后提醒”的时长优先级固定如下：用户本次明确说出的分钟数 > 用户设置的 `defaultSnoozeMinutes` > 初始默认值 `10` 分钟。服务将最终采用的分钟数写入提醒回执，便于用户在 IM 中核对。

### 3.8 撤销事务记录 `UndoTransaction`

```ts
type UndoOperationType =
  | "calendar_create"
  | "calendar_update"
  | "calendar_cancel"
  | "calendar_skip"
  | "calendar_pause"
  | "calendar_resume"
  | "calendar_terminate";

interface UndoTransaction {
  id: Id;
  operationType: UndoOperationType;
  targetType: "calendar_event" | "reminder_item";
  targetId: Id;
  beforeSnapshot: Nullable<Record<string, unknown>>;
  afterSnapshot: Nullable<Record<string, unknown>>;
  expiresAt: Nullable<ISODateTime>;
  undoneAt: Nullable<ISODateTime>;
  createdAt: ISODateTime;
}
```

`beforeSnapshot` 是撤销所需的权威恢复内容。撤销操作只能成功一次；删除或变更完成后 10 分钟过期。

### 3.9 会话回执 `ConversationReceipt`

会话回执用于消息回执中心和对话会话视图回看一次操作结果。

```ts
type ReceiptOperation =
  | "calendar_created" | "calendar_queried" | "calendar_changed"
  | "reminder_triggered" | "reminder_closed" | "reminder_snoozed"
  | "memo_recorded" | "memo_queried" | "operation_undone";

interface ConversationReceipt {
  id: Id;
  conversationId: Id;
  requestId: Id;
  operation: ReceiptOperation;
  targetType: Nullable<"calendar_event" | "reminder_item" | "memo">;
  targetId: Nullable<Id>;
  userUtterance: Nullable<string>;
  summary: string;
  before: Nullable<Record<string, unknown>>;
  after: Nullable<Record<string, unknown>>;
  affectedScope: Nullable<"this_occurrence" | "this_and_future" | "entire_series">;
  undoExpiresAt: Nullable<ISODateTime>;
  payload: Record<string, unknown>;
  createdAt: ISODateTime;
}
```

`summary` 面向用户展示；`conversationId` 让对话会话视图聚合本次会话的回执；`requestId` 用于 Function Calling 重试去重。`payload` 保存消息回执中心或 LLM 继续处理所需的结构化信息，例如提醒 ID、事件 ID、撤销事务 ID、变更前后字段；对写操作，它还必须足以重建首次成功返回的 `ServiceResult.data`，以便幂等重试直接返回首次结果。

## 4. SQLite 实体映射

| SQLite 表 | 领域模型 | 核心列 | 关系 |
| --- | --- | --- | --- |
| `calendar_events` | `CalendarEvent` | `id`, `title`, `start_at`, `end_at`, `kind`, `recurrence_*`, `status` | 被提醒事项和实例覆盖项引用。 |
| `reminder_items` | `ReminderItem` | `id`, `event_id`, `original_start_at`, `effective_start_at`, `trigger_at`, `status` | 属于一个日历事件。 |
| `weak_reminder_items` | `WeakReminderItem` | `id`, `event_id`, `original_start_at`, `trigger_at`, `status` | 与主提醒分离的轻提示。 |
| `occurrence_overrides` | `OccurrenceOverride` | `id`, `event_id`, `original_start_at`, `new_start_at`, `patch_json`, `status` | 周期实例的局部覆写。 |
| `memos` | `Memo` | `id`, `content`, `category`, `expires_at` | 24 小时临时记录。 |
| `application_settings` | `ReminderPreference` | `key`, `value_json`, `updated_at` | 应用配置面板维护的本地偏好；本期保存 `reminder.defaultSnoozeMinutes`。 |
| `undo_transactions` | `UndoTransaction` | `id`, `operation_type`, `target_id`, `before_snapshot_json` | 记录一次可逆日程操作。 |
| `conversation_receipts` | `ConversationReceipt` | `id`, `conversation_id`, `request_id`, `operation`, `target_id`, `summary`, `payload_json` | 为回执中心和会话视图提供历史。 |
| `notification_outbox` | `NotificationOutboxEntry` | `command_id`, `idempotency_key`, `status`, `attempt_count`, `created_at` | 与领域写入同事务持久化待投递通知；由适配层可靠分发。 |

建议约束与索引：

- `reminder_items(event_id, original_start_at)` 唯一，防止同一实例重复建立主提醒。
- `weak_reminder_items(event_id, original_start_at)` 唯一，防止同一实例重复建立弱提醒。
- `occurrence_overrides(event_id, original_start_at)` 唯一，保证同一实例只有一个生效覆盖项。
- `application_settings.key` 为主键；首次读取不到 `reminder.defaultSnoozeMinutes` 时返回 `10`，并可在首次写入时固化该值。
- `reminder_items(status, trigger_at)` 索引，供本地定时任务扫描。
- `conversation_receipts(created_at)` 索引，供回执中心倒序加载。
- `conversation_receipts(request_id)` 必须唯一。写服务必须在同一事务内插入该回执；若插入命中唯一约束，则回滚本次写入，读取该 `request_id` 对应的回执及 `payload`，并返回首次 `ServiceResult`。
- `notification_outbox(idempotency_key)` 必须唯一，并建立 `(status, created_at)` 索引以扫描待投递命令。
- 所有带 `event_id` 的子表使用外键关联 `calendar_events(id)`；删除事件时以事务清理关联提醒事项。

## 5. 服务接口

### 5.1 日历服务 `CalendarService`

```ts
interface CreateCalendarEventInput {
  title: string;
  startAt: ISODateTime;
  endAt?: ISODateTime;
  durationMinutes?: number;          // 与 endAt 二选一
  kind?: EventKind;
  timeZone: string;
  location?: string;
  description?: string;
  recurrence?: {
    frequency: RecurrenceFrequency;
    weekday?: number;
    monthDay?: number;
  };
  reminderAt?: ISODateTime;
  weakReminder?: boolean;
  conflictConfirmationToken?: ConfirmationToken;
}

interface CalendarQuery {
  rangeStart: ISODateTime;
  rangeEnd: ISODateTime;
  keyword?: string;
  statuses?: CalendarEventStatus[];
  cursor?: string;
  limit?: number;                   // 1..20，默认 20
}

interface OccurrencePage {
  items: Occurrence[];
  nextCursor: Nullable<string>;
}

interface CalendarService {
  create(context: ServiceContext, input: CreateCalendarEventInput): ServiceResult<{
    event: CalendarEvent;
    reminder: ReminderItem;
    weakReminder: Nullable<WeakReminderItem>;
    conflicts: Occurrence[];
  }>;

  get(eventId: Id): ServiceResult<CalendarEvent>;
  query(context: ServiceContext, query: CalendarQuery): ServiceResult<OccurrencePage>;
  find(context: ServiceContext, input: { query: string; rangeStart?: ISODateTime; rangeEnd?: ISODateTime }): ServiceResult<Occurrence[]>;
  getOccurrence(eventId: Id, originalStartAt: ISODateTime): ServiceResult<Occurrence>;
  ensureNextReminder(eventId: Id, afterOriginalStartAt: ISODateTime): ServiceResult<Nullable<ReminderItem>>;
}
```

接口规则：

- `create` 要求标题和发生时间；`time_block` 必须有 `endAt` 或 `durationMinutes`，`point` 只需触发时间。
- 仅支持每天、每周、每月周期；`endAt` 和 `durationMinutes` 互斥。
- 创建前检查时间段重叠、点状提醒落入时间段、同一时刻点状提醒三类冲突；首尾相接不算冲突。冲突时必须先返回候选和确认令牌，用户拒绝则不写入日程、提醒或创建回执。
- `query` 返回日程实例而非原始事件，按实际发生时间排序；范围为左闭右开且 `rangeEnd` 必须晚于 `rangeStart`。
- `find` 和 `getOccurrence` 返回的 `eventId + originalStartAt` 是后续变更的唯一目标。
- 创建成功时写入 `calendar_created` 回执；查询成功时写入 `calendar_queried` 回执。

### 5.2 提醒服务 `ReminderService`

```ts
interface ReminderService {
  schedule(context: ServiceContext, input: {
    eventId: Id;
    originalStartAt: ISODateTime;
    effectiveStartAt: ISODateTime;
    triggerAt: ISODateTime;
  }): ServiceResult<ReminderItem>;

  scanDue(context: ServiceContext): ServiceResult<Array<{ reminder: ReminderItem; occurrence: Occurrence }>>;
  listDue(context: ServiceContext): ServiceResult<Array<{ reminder: ReminderItem; occurrence: Occurrence }>>;
  markTriggered(context: ServiceContext, reminderId: Id): ServiceResult<ReminderItem>;
  close(context: ServiceContext, reminderId: Id): ServiceResult<ReminderItem>;
  snooze(context: ServiceContext, reminderId: Id, minutes?: number): ServiceResult<ReminderItem>;
  getDetails(reminderId: Id): ServiceResult<{ reminder: ReminderItem; occurrence: Occurrence }>;
  handleNotificationInteraction(context: ServiceContext, event: NotificationInteractionEvent): ServiceResult<ReminderItem>;
}
```

接口规则：

- 操作系统适配层的“本地定时任务触发”调用 `scanDue()`；它在同一事务中将到期主提醒标为 `pushed`、写回执、补齐下一周期提醒，并写入待投递的 `NotificationOutboxEntry`。领域服务不直接播放音频或显示系统通知。
- `close()` 只改变提醒状态，不改变日历事件。
- `snooze()` 只更新当前提醒的下一触发时间和状态，不修改原日程时间；`minutes` 省略时使用用户的默认推迟时长（首次为 10 分钟）；同一实例最多推迟 3 次，第 3 次后拒绝继续推迟。
- 有明确开始时间的事件在开始前固定 15 分钟生成弱提醒；弱提醒不要求响应，不能替代主提醒。
- 提醒触发、关闭和推迟均生成对应的会话回执。

### 5.3 日历变更服务 `CalendarChangeService`

```ts
interface ChangeCalendarEventInput {
  eventId: Id;
  originalStartAt: ISODateTime;
  scope: "this_occurrence" | "this_and_future" | "entire_series";
  patch: Partial<Pick<CalendarEvent,
    "title" | "startAt" | "endAt" | "timeZone" | "location" | "description"
  >>;
  conflictConfirmationToken?: ConfirmationToken;
}

interface CalendarChangeService {
  change(context: ServiceContext, input: ChangeCalendarEventInput): ServiceResult<{
    event: CalendarEvent;
    occurrence: Occurrence;
    affectedReminderIds: Id[];
    undoTransactionId: Id;
  }>;

  skip(context: ServiceContext, input: { eventId: Id; originalStartAt: ISODateTime; confirmationToken?: ConfirmationToken }): ServiceResult<{
    event: CalendarEvent;
    undoTransactionId: Id;
  }>;

  pause(context: ServiceContext, input: { eventId: Id; from?: ISODateTime; until: ISODateTime }): ServiceResult<{ undoTransactionId: Id }>;
  resume(context: ServiceContext, input: { eventId: Id }): ServiceResult<{ undoTransactionId: Id }>;
  terminate(context: ServiceContext, input: { eventId: Id; from: ISODateTime; confirmationToken?: ConfirmationToken }): ServiceResult<{ undoTransactionId: Id }>;
  cancel(context: ServiceContext, input: { eventId: Id; confirmationToken?: ConfirmationToken }): ServiceResult<{
    event: CalendarEvent;
    undoTransactionId: Id;
  }>;
}
```

接口规则：

- 所有变更先校验事件存在、实例存在、时间合法；变更日期/时间后必须同步受影响的提醒事项。
- 周期范围必须明确为“仅本次 / 本次及以后 / 整个系列”；目标或范围不唯一时不得执行。
- `skip` 仅跳过周期实例且幂等；单次事项的“跳过”等同取消，需用户确认。
- `pause` 仅暂停周期事项到明确日期；`terminate` 终止未来周期；`cancel` 删除单次事件或整个系列。删除、终止和单次跳过都需要确认。
- 时间变更后必须再次做冲突检查；只提示并由用户决定是否继续，绝不自动移动其他日程。
- 每个成功变更都保存撤销快照并写入 `calendar_changed` 回执。

### 5.4 事务撤销服务 `UndoTransactionService`

```ts
interface UndoTransactionService {
  undo(context: ServiceContext, input: { transactionId: Id }): ServiceResult<{
    transaction: UndoTransaction;
    restoredTarget: CalendarEvent | ReminderItem;
  }>;

  getLatestAvailable(targetId?: Id): ServiceResult<UndoTransaction>;
}
```

接口规则：

- `undo` 读取 `beforeSnapshot`，恢复目标实体及必要的关联提醒/覆盖项，并写入 `operation_undone` 回执。
- 已撤销、已过期或不存在的事务返回业务错误，不得重复恢复。
- 撤销行为本身不再产生可无限递归的撤销事务。

### 5.5 备忘录服务 `MemoService`

```ts
interface MemoService {
  create(context: ServiceContext, input: { content: string; category?: string }): ServiceResult<Memo>;
  query(context: ServiceContext, input?: { keyword?: string }): ServiceResult<Memo[]>;
}
```

接口规则：

- `content` 必填，不能为空白文本；服务应拒绝不在产品范围内的敏感凭证类内容。
- 固定保留 24 小时；不支持跨天模糊检索或长期个人记忆。
- 创建和查询均生成相应会话回执。

### 5.6 与操作系统适配层的领域边界

图中“本地定时任务触发”“通知交互事件捕获”“通知指令本地执行”和 `AudioPlayer` 都属于操作系统适配层，不是新的领域服务。为避免领域层直接依赖系统 API，双方通过下列 DTO 和端口协作：

```ts
type NotificationCommandType = "schedule" | "cancel" | "present";
type NotificationInteractionAction = "close" | "snooze";

interface NotificationCommand {
  commandId: Id;
  idempotencyKey: Id;
  type: NotificationCommandType;
  reminderId: Id;
  triggerAt: Nullable<ISODateTime>;
  title: string;
  body: Nullable<string>;
  createdAt: ISODateTime;
}

interface NotificationInteractionEvent {
  interactionId: Id;                 // 平台事件的稳定 ID；服务端去重键
  reminderId: Id;
  action: NotificationInteractionAction;
  requestedSnoozeMinutes: Nullable<number>; // action=snooze 可选；null 时使用默认值
  occurredAt: ISODateTime;
}

interface NotificationOutboxEntry extends NotificationCommand {
  status: "pending" | "dispatched";
  attemptCount: number;
  lastError: Nullable<string>;
  dispatchedAt: Nullable<ISODateTime>;
}

interface NotificationDispatchResult {
  acceptedCommandId: Id;             // 首次接受该 idempotencyKey 的 commandId
  status: "dispatched" | "duplicate";
}

interface NotificationCommandPort {
  dispatch(command: NotificationCommand): Promise<NotificationDispatchResult>;
}
```

- 任何会产生系统通知的领域写入，都必须在同一事务内插入 `status="pending"` 的 `NotificationOutboxEntry`，再提交业务实体、回执与 outbox；提交后不得直接把内存调用结果当作投递成功。
- 适配层的 outbox worker 在 App 启动时、每轮到期扫描后和失败重试时扫描 `pending` 命令并调用 `NotificationCommandPort`。端口返回 `dispatched` 或 `duplicate` 后，worker 才将该 outbox 行标记为 `dispatched`；调用失败时递增 `attemptCount`、记录 `lastError` 并保持 `pending`，以便下次恢复。适配层首次接受某个 `idempotencyKey` 后执行一次副作用并返回 `status="dispatched"`；后续相同键不得再次执行，返回首次的 `acceptedCommandId` 和 `status="duplicate"`。
- 适配层捕获通知操作后，构造 `NotificationInteractionEvent`，并将其 `interactionId` 原样作为 `ServiceContext.requestId` 调用 `ReminderService.handleNotificationInteraction()`。`action="snooze"` 时领域服务仅接收 `requestedSnoozeMinutes`，复用 `snooze()` 的默认时长、范围和次数校验，并以 `occurredAt` 计算 `triggerAt`；不得信任或接受适配层传入的绝对触发时间。领域服务通过该幂等键原子去重：重复事件返回首次结果，不再重复关闭、推迟、写回执或重建触发；领域服务仍是提醒状态的唯一写入者。
- `AudioPlayer` 只消费已触发提醒的播放指令，不拥有、也不修改 `ReminderItem`。

## 6. 给 LLM Function Calling 的工具契约

LLM 调度引擎使用以下工具，不直接访问 SQLite，也不直接调用操作系统适配层。

| Tool | 绑定服务 | 必填入参 | 返回数据 | 典型用途 |
| --- | --- | --- | --- | --- |
| `calendar_create` | 日历服务 | `context`, `title`, `startAt`, `timeZone` | `event`, `reminder`, `weakReminder`, `receipt` | “明早九点提醒我开会”。 |
| `calendar_query` | 日历服务 | `context`, `rangeStart`, `rangeEnd` | `events`, `receipt` | “今天有什么安排？” |
| `calendar_find` | 日历服务 | `context`, `query` | `occurrences` | 为修改或跳过定位某一次事件。 |
| `reminder_list_due` | 提醒服务 | `context` | `reminders` | “现在有什么提醒？” |
| `reminder_close` | 提醒服务 | `context`, `reminderId` | `reminder`, `receipt` | “知道了，不用再提醒。” |
| `reminder_snooze` | 提醒服务 | `context`, `reminderId` | `reminder`, `receipt` | “十分钟后再提醒我”；`minutes` 可选，省略时采用默认 10 分钟。 |
| `reminder_get_details` | 提醒服务 | `reminderId` | `reminder`, `occurrence` | “再详细说说。” |
| `calendar_change` | 日历变更服务 | `context`, `eventId`, `originalStartAt`, `scope`, `patch` | `occurrence`, `affectedReminderIds`, `undoTransactionId` | “把会议改到三点。” |
| `calendar_skip` | 日历变更服务 | `context`, `eventId`, `originalStartAt` | `event`, `undoTransactionId` | “这次不参加了。” |
| `calendar_pause` / `calendar_resume` | 日历变更服务 | `context`, `eventId`, `until` / `eventId` | `undoTransactionId` | “暂停到下周一。” |
| `calendar_terminate` / `calendar_cancel` | 日历变更服务 | `context`, `eventId`, `from` / `eventId` | `undoTransactionId` | “不再提醒 / 删除这个日程。” |
| `transaction_undo` | 事务撤销服务 | `context`, `transactionId` | `restoredTarget`, `receipt` | “撤销刚才的修改。” |
| `memo_create` | 备忘录服务 | `context`, `content` | `memo`, `receipt` | “帮我记一下停车位。” |
| `memo_query` | 备忘录服务 | `context` | `memos`, `receipt` | “我刚才记了什么？” |

统一 Function Calling 返回示例：

```json
{
  "success": true,
  "data": {
    "event": {
      "id": "evt_01",
      "title": "项目复盘会",
      "startAt": "2026-07-24T10:00:00+08:00",
      "endAt": "2026-07-24T11:00:00+08:00",
      "timeZone": "Asia/Shanghai",
      "kind": "time_block",
      "recurrenceFrequency": null,
      "status": "active"
    }
  },
  "receipt": {
    "id": "rcp_01",
    "conversationId": "conv_01",
    "requestId": "req_01",
    "operation": "calendar_created",
    "targetType": "calendar_event",
    "targetId": "evt_01",
    "summary": "已创建日程：项目复盘会",
    "payload": {},
    "createdAt": "2026-07-23T10:00:00+08:00"
  }
}
```

## 7. 关键协作流程

```text
LLM Function Calling
        │ 结构化参数
        ▼
本地领域服务 ──事务写入──> SQLite 实体、会话回执与通知 outbox
        │
        ├── 回执事件 ──> 消息回执中心 / 对话会话视图
        │
        └── 到期提醒 ──> 本地定时任务 / outbox worker ──> 通知交互事件捕获 / 通知指令本地执行 / AudioPlayer
```

1. 创建或变更日程时，日历服务/日历变更服务生成或更新提醒事项。
2. 本地定时任务从提醒事项中获取到期记录；提醒服务将其置为 `pushed` 并生成回执。
3. 领域层提交后向操作系统适配层下发通知命令；适配层展示通知、捕获用户点击或播放音频。关闭、推迟交互以 `NotificationInteractionEvent` 回流提醒服务。
4. 每次成功写操作都会产生会话回执；需要恢复时，以回执中的撤销事务 ID 调用事务撤销服务。

## 8. 已确认的策略规则

以下规则已由功能清单与 Issue #53 共同确认，并已反映到前述模型和接口：

| 主题 | 已确认规则 |
| --- | --- |
| 周期 | 仅支持每天、每周、每月；不支持复杂 RRULE、工作日组合、节假日、农历或跨时区。 |
| 创建与冲突 | 日程至少有标题和发生时间；时间段还必须有结束时间或时长。时间段重叠、点落入时间段、同一时刻点提醒均为冲突；首尾相接不冲突。冲突只提示并由用户决定，不自动移动其他日程。 |
| 主/弱提醒 | 主提醒在用户指定时间触发；有明确开始时间的日程在开始前 15 分钟触发弱提醒；弱提醒不替代主提醒。 |
| 稍后提醒 | 只改变当前提醒而不改日程；用户未说明时长时采用其默认值（首次为 10 分钟）；同一提醒实例最多推迟 3 次。 |
| 默认推迟 | 用户可在应用配置面板设置默认推迟时长；首次默认 10 分钟。显式说出的时长优先于设置值。 |
| 多提醒 | 多条同时到达时顺序播报；用户未指明目标时，必须先列出候选，不默认关闭任意一条。 |
| 周期变更 | 必须明确“仅本次 / 本次及以后 / 整个系列”；跳过只影响本次，重复跳过幂等。 |
| 高风险操作 | 删除、终止，以及把单次事项“跳过”（等同取消）必须二次确认。 |
| 撤销 | 最近一次删除或变更在 10 分钟内可撤销；恢复变更前状态。 |
| 临时便签 | 仅记录非敏感、无明确时间的小事；固定保留 24 小时，不构成长期个人记忆库。 |
| 回执与通道 | 语音负责短反馈和当前提醒处理；IM 保留完整回执、详情、候选项与撤销入口。 |

## 9. 仍待后续确认的实现策略

以下内容不改变已确认的产品行为，但图片、功能清单和 Issue #53 都没有给出足够精确的技术/交互口径，因此继续保留为后续设计项：

- 通知 outbox 的退避、最大重试次数、死信与用户可见告警策略；
- 回执、已过期便签和已撤销事务的物理清理周期；
- 确认令牌的生成/过期/失效机制与 IM 确认入口的具体协议；
- IM 查询分页的游标协议（产品已确认单页最多 20 条）；
- 语音播报多条提醒时的批次中断、失败恢复与播放优先级。
