# Proposal 03：已有日程变更与周期控制

> 战略基线：#51
> 用户故事输入：#46、#49
> 前置能力：Proposal 01 已经能够创建并查询日程
> 能力边界：本 Proposal 修改已有日程；不负责处理“收到、稍后、忽略”等当前提醒状态

## 动机 / 用户故事

我每天 17:30 固定写日报。今天因为路演延迟，我无法按原时间开始，只想说一句“把今天的日报推迟到晚上七点”：只改今天这一次，明天仍然是 17:30。如果系统把整个周期都改成晚上七点，我之后每天都需要再改回来；如果必须打开日历逐层选择“仅本次”，语音入口又失去了价值。

类似的临时变化也会发生在会议、缴费和其他周期事项中。单次日程可能需要改时间、改地点或删除；周期事项还可能只改本次、从本次以后修改、跳过一次、暂停一段时间或者永久停止。语音虽然能减少操作步骤，但也带来新的风险：系统可能找错事项、理解错时间，或者在用户只想修改一次时改变整个周期。

真实场景包括：

- 因为当天路演延迟，只把今天 17:30 的日报推迟到 19:00，明天恢复原时间；
- 临时把今天下午的会议改到晚上；
- 只把本周五的周报提醒改到周六，下周恢复原时间；
- 从下个月开始，把每月缴费提醒统一提前一天；
- 出差期间暂停每晚的学习提醒，回来后继续；
- 本周临时不需要某项周期提醒，但不希望删除整个周期；
- 取消一个已经不存在的安排，并在删错时恢复；
- 修改后的时间与另一条日程冲突，需要用户决定是否保留重叠。

**共同需求一句话提炼**：用户可以通过语音安全地修改已有日程，并清楚知道这次操作影响哪一条、哪一次或哪一段周期。

## 目标用户

- 愿意使用语音管理个人日程的用户；
- 日常存在临时改期、取消和跳过需求；
- 拥有每天、每周或每月重复事项；
- 经常处在不方便打开屏幕查找和修改日程的场景；
- 希望快速操作，但不能接受系统静默误改整个周期。

## 现有做法及其不足

| 现有做法 | 不足 |
|---|---|
| 删除原日程后重新创建 | 容易遗漏地点、备注和周期信息 |
| 语音直接执行所有修改 | 可能找错事项或误改整个周期 |
| 每次修改都要求多轮确认 | 虽然安全，但会失去语音操作的效率 |
| 周期事项只提供“修改全部” | 用户无法只改本次或从某次以后修改 |
| 删除后无法恢复 | 一次识别错误可能造成不可逆结果 |
| 系统自动解决时间冲突 | 可能违背用户保留重叠安排的真实意图 |

## 本期范围与明确不做

### 本期范围

1. 用户可以通过语音修改唯一命中的单次日程，包括日期、时间、标题、地点和备注。
2. 如果有多条日程可能符合用户描述，系统先询问或通过 IM 提供受控选择，不猜测目标。
3. 用户可以修改周期事项中的某一次，并保持其他周期实例不变。
4. 用户可以选择修改“本次”“本次及以后”或“整个系列”。
5. 用户表达中已经明确范围时，系统不重复追问；范围不明确时必须先确认。
6. 用户可以跳过某一次周期事项，并获知下一次提醒时间。
7. 当“跳过”命中单次事项时，系统说明“跳过等同于取消这条日程”，取得用户确认后才执行，并提供撤销。
8. 对同一个周期实例重复说“跳过”时，系统告知“本次已经跳过”，不重复变更、不生成重复操作回执。
9. 用户可以暂停周期事项至明确日期，到期后恢复原周期。
10. 用户可以终止周期事项，使其不再产生后续提醒。
11. 用户可以删除单次日程或整个周期；删除和终止等高风险操作必须明确确认。
12. 修改后的时间与其他日程重叠时，系统提示冲突，由用户决定是否继续，不自动替用户调整。
13. 修改、跳过、暂停、终止和删除完成后，系统使用语音说明结果，并留下 IM 回执。
14. IM 回执只展示适用于当前操作的信息，不要求不存在的“新安排”或“下一次发生时间”。
15. 用户可以在有限的撤销窗口内恢复刚才的修改、跳过、暂停、终止或删除操作；具体时长留待交互设计确定。

不同操作的 IM 回执内容如下：

| 操作 | 回执必须说明 | 撤销后恢复什么 |
|---|---|---|
| 修改 | 原安排、新安排、影响范围、下一次发生时间 | 恢复原安排和原周期范围 |
| 跳过周期实例 | 被跳过的实例、影响范围、下一次发生时间 | 恢复该周期实例 |
| 暂停 | 暂停区间、自动恢复时间 | 取消暂停，恢复原周期 |
| 终止 | 终止对象、生效时间、影响范围，并明确“无下一次” | 恢复终止前的周期状态 |
| 删除 | 被删除事项、删除范围，并明确“无下一次” | 恢复被删除的事项或周期 |

### 本期明确不做

- 创建新的日程或周期提醒，由 Proposal 01 负责；
- 查询今天、明天或其他时间范围内的日程，由 Proposal 01 负责；
- 处理“收到了”“稍后提醒”“忽略当前提醒”等提醒状态，由 Proposal 02 负责；
- 完整 RRULE 和复杂周期规则；
- 法定节假日、工作日历和农历规则；
- 一次修改多条无关日程；
- “清空我这个月所有安排”等批量危险操作；
- 自动替用户选择修改范围；
- 自动移动其他日程来解决冲突；
- 修改其他人的日程或共享日历；
- 根据位置、传感器或外部消息自动修改日程；
- 提供面向用户的日历 GUI；
- 在本 Proposal 中定义数据库字段、接口和实现方案。

## 关键决策与依据

| 决策点 | 备选方案 | 选择与理由 |
|---|---|---|
| Proposal 边界 | 按操作入口划分；按被修改对象划分 | 选择按对象划分：只要改变已有日程、周期实例或周期规则，都进入 Proposal 03 |
| 目标识别 | 命中第一条直接修改；存在歧义时询问 | 选择歧义时询问：找错事项比多问一次造成的损失更大 |
| 周期范围 | 默认修改全部；所有操作都询问；表达明确时执行、模糊时确认 | 选择第三种：兼顾语音效率和周期操作安全 |
| 确认方式 | 所有修改都确认；所有修改都直接执行；按风险分层 | 选择按风险分层：明确的单次修改可执行后回读，高风险和范围不清操作先确认 |
| 跳过与删除 | 统一视为删除；按事项类型区分 | 选择按事项类型区分：周期事项跳过只影响一个实例；单次事项没有下一次，跳过等同取消，必须说明并确认 |
| 终止与删除 | 统一删除；保留不同语义 | 选择保留不同语义：终止表示停止后续周期，删除表示移除指定日程 |
| 时间冲突 | 拒绝修改；自动调整；提示后由用户决定 | 选择提示后决定：重叠可能是用户的真实安排，系统不替用户取舍 |
| 操作反馈 | 只语音播报；只发 IM；语音摘要加 IM 回执 | 选择语音摘要加 IM 回执：用户可以立即知道结果，也能事后核对 |
| 误操作恢复 | 修改前反复确认；修改后提供撤销 | 选择风险确认加可撤销：高风险动作先确认，同时保留恢复能力 |

## 基本概念与信息结构

| 概念 | 定义 |
|---|---|
| 单次日程 | 只发生一次的日程，例如“今天下午三点和 Alex 开会” |
| 周期事项 | 按固定规则重复发生的事项，例如“每周五下午五点提交周报” |
| 周期实例 | 周期事项中的某一次，例如“本周五这一次周报” |
| 本次 | 只改变当前指定的一个周期实例 |
| 本次及以后 | 从指定实例开始改变后续安排，之前的实例保持不变 |
| 整个系列 | 改变该周期事项的整体规则；系统必须明确反馈影响范围 |
| 跳过 | 对周期事项只影响一个实例，下一次仍按原周期继续；对单次事项等同取消，确认后执行 |
| 暂停 | 在明确时间范围内停止产生提醒，到期后恢复 |
| 终止 | 停止周期事项的后续安排，属于高风险操作 |
| 删除 | 移除指定日程或周期系列，属于高风险操作 |
| 撤销 | 在有限时间内恢复最近一次变更前的状态 |

```text
用户说出变更要求
  → 识别目标日程
  → 判断操作类型和影响范围
  → 目标或范围不明确时询问
  → 高风险操作显式确认
  → 检查时间冲突
  → 执行变更
  → 语音反馈结果
  → IM 留下回执和撤销入口
```

## 原型 / Demo

Demo 以语音对话和 IM 回执呈现，不依赖日历页面。

### 行为例子一：路演延迟，只修改今天的日报时间

- **现状**：用户每天 17:30 固定写日报。今天的路演延迟，无法按原时间开始，但明天仍希望按 17:30 执行。
- **用户说**：“把今天的日报推迟到晚上七点。”
- **提议后**：系统只修改今天这一个周期实例，并反馈“已把今天的日报改到 19:00，明天仍然是 17:30”。
- **IM 回执**：展示原时间 17:30、新时间 19:00、影响范围“仅本次”和下一次时间“明天 17:30”。
- **边界**：因为用户明确说了“今天”，系统不重复询问范围。
- **非法情况**：系统不能把之后每天的日报都改到 19:00。

### 行为例子二：周期范围不明确

- **用户说**：“把每周五的周报改到星期六上午。”
- **提议后**：系统询问“只改本周、从本周开始以后都改，还是修改整个系列？”
- **用户说**：“以后都改。”
- **系统反馈**：“将从本周开始改为每周六上午。上午几点？”
- **边界**：时间和范围都明确后才能执行。
- **非法情况**：系统不能自行假设为本次或整个系列。

### 行为例子三：跳过一次

- **用户说**：“这周的周报跳过一次。”
- **提议后**：系统跳过本周实例，并反馈“已跳过本周，下次仍然是下周五下午五点”。
- **IM 回执**：说明影响范围为“仅本次”，并提供撤销。
- **重复表达**：用户再次说“这周周报跳过一次”时，系统反馈“本周这次已经跳过”，不重复变更，也不生成第二条操作回执。
- **非法情况**：系统不能暂停或终止整个周期。

### 行为例子四：跳过单次事项

- **用户说**：“跳过明天下午三点和 Alex 的会。”
- **提议后**：系统说明“这是单次日程，跳过会取消这场会议，确定吗？”
- **用户确认后**：系统取消该日程，生成删除回执并提供撤销。
- **边界**：用户没有确认时，日程保持不变。
- **非法情况**：系统不能把单次事项当成周期实例直接静默跳过。

### 行为例子五：终止周期事项

- **用户说**：“以后不用再提醒我提交周报了。”
- **提议后**：系统确认“这会停止之后所有周报提醒，确定终止吗？”
- **用户说**：“确定。”
- **系统反馈**：“已终止周报周期提醒，可以在撤销窗口内恢复。”
- **非法情况**：用户确认前不得停止后续提醒。

### 行为例子六：冲突与撤销

- **用户说**：“把今天和 Alex 的会改到下午四点。”
- **系统发现**：下午四点已有“项目评审”。
- **提议后**：系统反馈“下午四点已有项目评审，是否仍然把 Alex 的会改到四点？”
- **用户确认后**：系统保留两条重叠日程，并在 IM 中说明冲突。
- **用户说**：“撤销刚才的修改。”
- **结果**：Alex 的会议恢复到原时间，项目评审保持不变。
- **非法情况**：系统不能自动移动项目评审或取消其中一条日程。

## 验收标准

| 场景 | 预期结果 |
|---|---|
| 唯一命中的单次日程 | 按用户要求修改，并回读变更结果 |
| 多条日程可能匹配 | 不执行修改，先要求用户选择目标 |
| 用户明确说“这周五这一次” | 只修改对应周期实例 |
| 用户明确说“从这周开始” | 修改本次及以后，之前的实例保持不变 |
| 用户明确说“整个系列” | 修改整个周期，并明确反馈影响范围 |
| 周期范围没有说清 | 执行前询问本次、本次及以后或整个系列 |
| 用户跳过一次周期事项 | 只跳过指定实例，并说明下一次提醒时间 |
| 用户重复跳过同一周期实例 | 告知“本次已经跳过”，不重复变更或生成重复操作回执 |
| 用户对单次事项说“跳过” | 说明跳过等同取消，取得确认后执行并提供撤销 |
| 用户暂停至某日 | 暂停期间不再提醒，到期后恢复原周期；撤销后立即恢复原周期 |
| 用户永久停止周期 | 显式确认后终止后续提醒 |
| 用户删除日程 | 显式确认后删除，并提供撤销 |
| 修改后发生时间冲突 | 提示冲突，获得用户确认前不执行 |
| 用户在撤销窗口内撤销 | 修改、跳过、暂停、终止或删除均恢复操作前的内容和周期范围 |
| 操作执行完成 | 语音反馈结果，并按操作类型生成包含适用字段的 IM 回执 |
| 用户在提醒期间说“十分钟后提醒” | 交由 Proposal 02，只改变提醒状态 |
| 用户在提醒期间说“把会议改到八点” | 进入 Proposal 03，修改底层日程 |

最低通过条件：

1. 系统不会在目标日程不明确时猜测并执行。
2. 周期事项变更后，用户能知道影响的是本次、本次及以后还是整个系列。
3. 跳过一次不会改变下一次周期安排，重复跳过同一实例不会产生重复变更或回执。
4. 单次事项的“跳过”会被解释为取消，并且不会未经确认直接执行。
5. 删除、终止和范围不明确的周期操作不会未经确认直接执行。
6. 修改产生冲突时，系统不会自动替用户调整其他日程。
7. 每次成功变更都有语音反馈，以及与操作类型一致的 IM 回执。
8. 用户能够撤销最近一次仍在撤销窗口内的修改、跳过、暂停、终止或删除。
9. Proposal 02 的提醒状态处理不会被误当成日程变更。

## 待评审问题

1. “本次及以后”和“整个系列”是否都需要在 MVP 中保留，还是先收敛为“仅本次”和“以后全部”？
2. 暂停周期事项是否进入本期，还是只支持跳过一次和永久终止？
3. 哪些单次修改可以执行后回读，哪些必须执行前确认？
4. 删除和撤销窗口的具体时长，由后续交互设计确定。
5. 时间冲突只提示开始时间重叠，还是还需要判断日程持续时间？
