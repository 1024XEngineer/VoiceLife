# Proposal: 已有日程变更与周期控制

## 动机 / 用户故事

| 场景 | 描述 | 痛点 |
| --- | --- | --- |
| 场景1：临时调整单次日程 | 我每天的日程都是17:30写日报，但是今天我要进行路演，需要延迟到19:00 | 手动修改一次提醒可能会影响后续提醒，或者需要我自己后续手动改回，很不便利 |
| 场景2：因外部原因变更日程时间 | 我今天本来是10:00开会的，但是会议室坏了需要维修，下午才能修好，故会议推迟到下午，需要修改我的日程 | 我需要打开日程安排，自己手动修改我的日程 |
| 场景3：跳过一次周期事项 | 我每周五都要去跑步，但是这周我有事要忙，不能跑步了，我想要跳过本周事项，但是不修改周期日程 | 我需要手动打开日程安排，本周关闭这个日程，后续再打开 |

## 目标用户
- 需要暂时修改或者永久修改自己的日程，并希望能通过语音交互而不是进行繁琐点击交互
- 需要跳过一次日程，但是手动操作繁琐，且容易遗漏

## 现有做法及其不足

| 当前做法                      | 不足           |
| ------------------------- | ------------ |
| 手动变更一次日程后续再自己改回来，或是手动变更修改 | 需要手动变更，缺乏自动化 |
| 手动跳过一次（自己关闹钟）             | 操作繁琐，且容易遗忘   |

## 本期范围与明确不做

做：
- 用户通过语音交互修改只进行一次的日程，修改成功后播报并IM回执
- 用户通过语音将跳过本次日程，跳过成功后播报并IM回执
- 用户通过语音交互只修改一次周期实例，修改成功后播报并IM回执
- 用户通过语音交互永久修改一项周期事项，修改成功后播报并IM回执
- 用户通过语音交互进行如永久删除一项周期事项等高风险操作时，必须显示确认再进行修改
- 用户通过语音交互修改日程时有歧义，应进行询问确认
- 用户通过语音交互修改一个不存在的日程时，语音提示不存在
- 用户修改一个日程与已有日程冲突时，提示冲突，并按冲突处理的方式进行处理
- 用户可以通过语音交互撤销修改刚刚修改的日程，撤销成功后播报并IM回执

不做：
- 批量修改多项日程
- 创建新的日程或提醒（Proposal 01）
- 修改提醒后产生冲突后的冲突处理（Proposal 01）
- 对已有提醒表达知道了或延迟（Proposal 02）
- 自动为用户选取修改范围
- 匹配完整的工作日和节假日周期

## 关键决策与依据

用户修改日程时有歧义
- 备选方案 A：询问细节进行确认
- 备选方案 B：命中最匹配的一项

选择 A，理由：需要用户阐明，避免改错

时间冲突时
- 备选方案 A：用户自己决定，进行冲突处理
- 备选方案 B：拒绝修改

选择 A，理由：冲突应由用户自己安排，事项可能可以重叠

## 基本概念与信息结构

### 核心概念

| 概念    | 含义                                      |
| ----- | --------------------------------------- |
| 单次日程  | 只发生一次的日程，例如“今天下午三点开会”                   |
| 周期事项  | 按固定规则重复发生的事项，例如“每周五下午五点提交周报”            |
| 周期实例  | 周期事项中的某一次，例如“本周五这一次周报”                  |
| 高风险操作 | 可能影响后续安排，有一定影响范围等事项                     |
| 本次    | 只改变当前指定的一个周期实例                          |
| 本次及以后 | 从指定实例开始改变后续安排，之前的实例保持不变                 |
| 跳过    | 对周期事项只影响一个实例，下一次仍按原周期继续；对单次事项等同取消，确认后执行 |
| 删除    | 移除指定日程或周期系列，属于高风险操作                     |
| 撤销    | 在有限时间内恢复最近一次变更前的状态                      |

### 数据模型简图

```text
用户说出变更要求
  → 识别目标日程
  → 判断操作类型和影响范围
  → 目标或范围不明确时询问
  → 高风险操作显式确认
  → 检查时间冲突
  → 执行变更
  → 语音反馈结果/ IM 留下回执
```

## 原型 / Demo

Todo

## 验收标准

例子：
- 用户语音交互修改一次单次事项时无冲突，完成修改，语音回复修改成功，并发送IM回执
- 用户语音交互修改一个周期实例，而不影响后续时，完成修改，语音回复单次修改成功，并发送IM回执
- 用户语音交互修改一个周期事项，并影响后续时，完成修改，语音回复周期事项修改成功，并发送IM回执
- 用户语音交互删除一个周期事项时，语音回复要求显示确认，用户确认则完成修改，语音回复删除该事项并发送IM回执，若未确认则不删除
- 用户语音交互要求撤销之前修改的事项时，完成修改，语音回复撤销成功并发送回执
- 用户通过语音交互修改日程时有歧义，应进行询问确认信息，确认为唯一日程后完成修改，语音回复修改成功，并发送IM回执
- 用户语音交互修改一个日程与已有日程冲突时，语音提示冲突，并按冲突处理的方式进行处理，不进行修改
