# 日程管理模块需求与设计文档

本模块皆在定义 **VoiceLife**（时间管理产品名称）的日程管理模块，我参考了业界相关开源项目，并结合自身实际业务场景，提出一套产品需求与设计。



## 0. 参考对象

1. oh-my-task（Github个人开源项目）：https://github.com/qq33357486/oh-my-task
1. Agentscope-example(Github 开源项目)：https://github.com/AlfredChaos/agentscope-example



## 1. 核心目标

为VoiceLife引入日程管理能力，使其能够跨越单次会话的限制，结构化持久存储在用户本地。这些数据将在后续交互过程中通过 MCP Tool 自动操作，从而实现个人日程管理。



## 2. 核心概念定义

定义该模块核心实体，以便更精确的匹配我们的业务模型：

- ScheduleID（日程 ID）

  - 定位每一个日程，每一个日程都拥有唯一的 ID

  - 一个日程下面包含有时间、地点、事件状态、备注等

- ReminderID（提醒 ID）

  - 它是日程的提醒实体，如果该日程需要提醒则ReminderID 有值。

  - 一个 ScheduleID 对应多个 reminderID(一对多)
  - 虽然一对一（一个 ScheduleID 对应一个 ReminderID)也可以实现多次提醒，但是后续用户自行调整/拓展提醒就不方便了。
  - 例如：用户说“至少提前提醒我 N 次”、“不要到点提醒我”。



## 3. 核心业务流程

该模块的运作主要分为二大流程：

### 流程一：对日程进行增删改查

此流程允许用户对自己的日程进行增删改查。

1. **创建功能：**用户可以通过语音交互完成日程的**创建**。创建完成后，该日程将拥有唯一的 ScheduleID 以及和其关联的 Event。
   - **核心流程：**ASR -> LLM -> Schedule Create Tool -> DB -> LLM -> TTS
   - **提醒功能：**如果该日程需要提醒的话，需在日程创建完毕后，创建提醒，然后关联此日程即ReminderID。

2. **查询功能：**用户可以通过语音交互完成日程的**查询**。用户通过描述事件的特征/维度如：时间、状态、地点、事件等信息来完成日程的查询。

   - **核心流程：**ASR -> LLM -> Schedule Query Tool  -> LLM -> TTS

3. **改动功能：**用户可以通过语音交互完成日程的**更改**。用户可以对已有的日程的特征进行修改：如时间、状态、地点、事件等信息。

   - **核心流程：**ASR -> LLM -> Schedule Query Tool -> LLM ->Schedule Update Tool -> LLM -> TTS
   - **二次确认流程：**ASR -> LLM -> Schedule Query Tool **-> LLM -> TTS -> ASR -> LLM ->** Schedule Update Tool -> LLM -> TTS

4. **删除功能：**用户可以通过语音交互完成日程的**删除**。用户通过对日程的特征描述删除日程，删除后不能再对该 ScheduleID 进行调整。

   - **核心流程：**ASR -> LLM ->  Schedule Query Tool -> LLM -> Schedule Delete Tool -> LLM -> TTS
   - **二次确认流程：**ASR -> LLM ->  Schedule Query Tool -> **LLM -> TTS-> ASR -> LLM ->** Schedule Delete Tool -> LLM -> TTS
   - **提醒功能：**在日程删除完成后，需删除改日程相关联的提醒即 ReminderID，防止数据不一致。

   

### 流程二： 对日程操作撤销

允许用户对最近操作进行撤销，系统记录用户的最近操作。

1. **记录：** 用户对日程进行增删改完成后记录增删改前的数据状态。仅记录一定量的操作，防止记录过量操作对后续模型上下文造成一定压力。

   **核心流程：**ASR -> LLM -> Schedule Create Tool -> DB -> **Record Operation** -> LLM -> TTS

2. **撤销：**通过记录的操作数据，当用户需要撤销之前的操作时；首先查询出最近的操作以及操作前的数据状态，让模型推断用户要撤销的操作，最终完成撤销。

   **核心流程：**ASR -> LLM -> Query Recent Operation Tool -> LLM -> Undo Operation Tool -> LLM -> TTS

   **二次确认流程：**ASR -> LLM -> Query Recent Operation Tool -> **LLM -> TTS -> ASR -> LLM ->** Undo Operation Tool -> LLM -> TTS



## 4. 接口设计

> 以下接口均为函数式接口（无 HTTP 请求）供大模型使用

### 1. 接口总览

**日程管理（Schedule Management）**

create_schedule——创建日程

delete_schedule——删除日程

update_schedule——修改日程

query_schedule——查询日程

record_schedule_operation——记录日程操作

query_recent_schedule_operation——查询最近日程操作

undo_schedule_operation——回滚/撤销日程操作



### 2.详细接口参数

#### 1）创建日程

**工具描述：**创建一条日程。如果时间冲突且未忽略冲突，则仅返回冲突列表且不创建。

**入参：**

- **event(String，必填）:** 事件标题
- **start_time(String， 可选）：**事件开始时间
- **end_time(datetime，可选）：**事件结束时间
- **location（datetime，可选）：**事件地点

- **notes（String，可选）：**事件备注
- **ignore_conflict(Boolean, 可选，默认 False）：**是否忽略与其他日程的时间冲突

**出参：**

**message（String）：**对此次创建的一些说明，比如：”created successful“。

**schedule（Schedule 实体对象）：**创建完成后的 Schedule 实体对象。

**conflicts（Schedule[]）：**创建成功时此字段为空，日程冲突时为冲突日程实体。

**error（String）：**无错误时为空。和 message 字段的区别是，error 字段更加突出，message 字段适合常规信息。

#### 2）查询日程

**工具描述：**根据日程 ID、关键词或时间范围查询日程。多个查询条件之间为 AND 关系，结果按开始时间升序排列。不传任何参数默认查询所有日程。

**入参：**

- **schedule_id(Number，可选）：**日程 ID，提供后按 ID 精确查询
- **keyword(String，可选）：**事件标题关键词，支持模糊匹配
- **start_from(datetime，可选）：**开始时间范围的下限
- **start_to(datetime，可选）：**开始时间范围的上限
- **status(String，可选，默认 active）：**状态筛选；all 表示全部状态，cancelled 表示已取消日程
- **limit(Number，可选，默认 10，最大 50）：**返回条数
- **offset(Number，可选，默认 0）：**分页偏移量

**出参：**

**schedules（Schedule[]）：**符合条件的日程列表。

**total（Number）：**符合条件的日程总数，不受 limit 和 offset 影响。

**error（String）：**调用失败时的错误信息；无错误时为空。

#### 3）修改日程

**工具描述：**修改一条已有日程。调用前应先查询并确定目标日程的 schedule_id。如果时间冲突且未忽略冲突，则仅返回冲突列表且不修改任何字段。

**入参：**

- **schedule_id(Number，必填）：**要修改的日程 ID
- **event(String，可选）：**新的事件标题
- **start_time(datetime，可选）：**新的开始时间
- **end_time(datetime，可选）：**新的结束时间
- **location(String，可选）：**新的地点
- **notes(String，可选）：**新的备注
- **reminder_id(Number，可选）：**关联的提醒 ID
- **ignore_conflict(Boolean，可选，默认 False）：**是否忽略时间冲突

**出参：**

**message（String）：**对此次修改的一些说明。

**schedule（Schedule 实体对象）：**修改后的完整日程；未修改时为空。

**conflicts（Schedule[]）：**修改后发生冲突的日程；无冲突时为空数组。

**error（String）：**调用失败时的错误信息；无错误时为空。

#### 4）删除日程

**工具描述：**删除/取消一条日程。调用前应先查询并确定目标日程的 schedule_id。该接口不会自动删除关联提醒。

**入参：**

- **schedule_id(Number，必填）：**要删除的日程 ID

**出参：**

**schedule_id（Number）：**被删除的日程 ID。

**deleted（Boolean）：**是否成功删除日程。

**error（String）：**调用失败时的错误信息；无错误时为空。

#### 5）查询最近日程操作

**工具描述：**查询当前用户最近 10 条日程操作。如需撤销，应先调用该接口找到用户想撤销的操作记录。

**入参：**

无。

**出参：**

**operations（Operation[]）：**可撤销的操作记录（包含操作和日程状态），按操作时间倒序排列。

**error（String）：**调用失败时的错误信息；无错误时为空。

#### 6）撤销日程操作

**工具描述：**撤销当前用户最近 15 分钟内指定的一条日程创建、修改或删除操作。

**入参：**

- **operation_id(Number，必填）：**要撤销的操作记录 ID，由 query_recent_operations 接口获取

**出参：**

**undone（Boolean）：**是否成功撤销。

**operation（Operation 实体对象）：**被撤销的操作信息（撤销前的状态）；无操作时为空。

**schedule（Schedule 实体对象）：**撤销完成后的日程（撤销后的状态）；撤销创建操作时为空。

**error（String）：**操作不存在、已过期或调用失败时的错误信息；无错误时为空。



## 5. 数据库表结构设计

### 1. 日程表 （Schedule）

存储所有日程

| 字段名 | 类型    | 约束              | 说明     |
| ------ | ------- | ----------------- | -------- |
| id | integer | PK，AutoIncrement | 自增主键 |
| event | varchar(100) | NOT NULL | 事件标题 |
| start_time | datetime | NULL | 事件开始时间 |
| end_time | datetime | NULL | 事件结束时间；为空表示仅时间点、无持续时长 |
| location | varchar(100) | NULL | 事件地点 |
| notes | varchart（200） | NULL | 事件备注 |
| reminder_id | integer | NULL | 关联的提醒 ID；为空表示无提醒 |
| status | tinyint         | NOT NULL，DEFAULT 1 | 日程状态：1（有效）或 2（已取消） |
| created_at | datetime | NOT NULL | 日程创建时间 |
| updated_at | datetime | NOT NULL | 日程最后修改时间 |

### 2. 操作记录表（OperationRecord）

记录日程创建、修改和删除操作，用于支持最近操作查询和操作撤销。

| 字段名 | 类型 | 约束 | 说明 |
| ------ | ---- | ---- | ---- |
| id | integer | PK，AutoIncrement | 操作记录自增主键 |
| type | tinyint      | NOT NULL | 操作类型：1（create）、2（update ）或 3（delete） |
| schedule_id | integer | NOT NULL | 涉及的日程 ID |
| schedule_event | varchar(100) | NOT NULL | 操作时日程的事件标题；delete 操作为删除前的标题 |
| operated_at | datetime | NOT NULL | 操作发生时间 |
| previous | json | NULL | 操作前日程的完整快照；create 操作为 NULL，update/delete 保存完整 Schedule 对象 |













