# 日程管理模块需求与设计文档

本模块用于定义 **VoiceLife**（时间管理产品名称）的日程管理能力。日程数据结构化持久化在用户本地，并供上层交互适配层调用，支持用户使用自然语言完成日程创建、查询、修改、删除和撤销。

## 0. 参考对象

1. oh-my-task（Github 个人开源项目）：https://github.com/qq33357486/oh-my-task
1. Agentscope-example（Github 开源项目）：https://github.com/AlfredChaos/agentscope-example

## 1. 核心目标

为 VoiceLife 引入可持久化的日程管理能力，支持以下场景：

- 创建一次性日程
- 创建每天、每周、每月或每年的周期日程
- 查询指定时间范围内的具体日程实例
- 查询未来尚未生成的周期日程
- 修改或跳过周期日程中的某一次实例
- 修改或取消整条周期规则
- 记录日程操作并支持撤销

## 2. 核心概念定义

### 2.1 ScheduleRule（周期规则）

ScheduleRule 表示一条周期日程定义，保存周期规则、默认标题、地点、备注、首次生效时间和实例生成进度。

例如：

```text
每周一 10:00 开会
```

系统只需要保存一条 ScheduleRule，不需要一次性创建所有未来日程。

### 2.2 Schedule（日程实例）

Schedule 表示一次具体发生的日程。

- 一次性日程：直接创建一条 Schedule，rule_id 为空
- 周期日程：由 ScheduleRule 按时间窗口生成一条或多条 Schedule

周期实例在 Schedule 中只保存最终实际发生时间 start_time。未生成实例的单次调整，则通过 ScheduleException 的 rule_id + original_start_time 定位。

例如：

```text
ScheduleException.original_start_time = 2027-02-01
Schedule.start_time                  = 2027-02-02 14:00:00
```

表示原本在 2 月 1 日 10:00 的会议被改到了 2 月 2 日 14:00。

### 2.3 ScheduleException（单次例外）

ScheduleException 用于记录周期规则中某一次与默认规则不同的情况：

- 跳过某一次
- 修改某一次的时间
- 修改某一次的标题、地点或备注

已生成的实例可以直接更新 Schedule；尚未生成的实例只保存 ScheduleException，待以后生成时应用。

### 2.4 OperationRecord（操作记录）

OperationRecord 记录对 Schedule、ScheduleRule 和 ScheduleException 的创建、修改、删除及撤销操作，用于最近操作查询和撤销。

## 3. 核心业务流程

### 流程一：创建日程

#### 3.1 创建一次性日程

用户提供事件和时间后，直接创建一条 Schedule：

```text
语音交互层 -> 日程应用服务 -> DB -> 语音交互层
```

一次性日程的 rule_id 为空。

#### 3.2 创建周期日程

用户说：

```text
妈妈生日，每年 5 月 1 日 10:00
```

系统执行：

1. 创建一条 ScheduleRule
2. 根据规则生成未来时间窗口内的 Schedule
3. 所有实例生成成功后更新 generated_until
4. 记录创建操作

默认生成窗口为未来 30 天，具体窗口长度由系统配置决定。

首次创建、设备启动、设备从休眠恢复、时间同步以及后台补偿任务都会调用统一的实例生成逻辑，不为每一条规则创建独立定时器。

实例生成必须满足：

- 生成前检查 rule_id 对应的既有 Schedule 和 ScheduleException，防止重复
- 生成每次实例前先处理 ScheduleException
- Schedule 写入和 generated_until 更新在同一事务中完成
- 只有所有候选实例处理成功后才推进 generated_until

generated_until 使用排他上界语义：小于 generated_until 的所有原始发生时间均已处理，下一次从该边界继续计算。被 skip 的实例也视为已处理。

### 流程二：补充周期实例

系统通过一个统一的后台任务维护未来时间窗口：

```text
当前时间 + 未来 30 天
```

每天检查时只补窗口尾部新增的实例，不重新插入整个窗口。

例如：

```text
第一天：生成 8 月 10 日至 9 月 9 日
第二天：只补 9 月 10 日
```

如果设备在补偿时间点处于关机状态，设备下一次启动时重新执行窗口检查。

### 流程三：查询日程

查询结果统一面向具体发生的日程实例。

#### 3.1 有时间条件且范围在生成窗口内

系统先确保周期规则已经生成到查询结束时间，然后直接查询 Schedule 表。

```text
查询条件 -> 确保生成窗口 -> 查询 Schedule
```

一次性日程和周期日程实例都通过同一张 Schedule 表返回。

#### 3.2 有时间条件但范围超过生成窗口

例如用户查询六个月后的日程：

1. 查询已经生成的 Schedule
2. 查询可能在范围内发生的有效 ScheduleRule
3. 根据规则计算尚未生成的实例
4. 查询原始时间或调整后时间与查询范围相交的 ScheduleException
5. 使用 Schedule 的已生成记录和 ScheduleException 的 rule_id + original_start_time 去重
6. 应用例外后，再按照实际 start_time 和其他查询条件过滤
7. 合并、排序和分页

未落库的计算结果作为临时查询实例返回，操作该实例时使用 rule_id + original_start_time 定位。

#### 3.3 没有时间条件

周期日程的发生次数可能是无限的，因此不能无限展开所有实例。

默认行为是查询：

```text
当前时间至未来 30 天
```

该查询包含：

- 时间窗口内的一次性日程
- 时间窗口内的周期日程实例

如果用户需要查看周期规则定义，应使用规则查询模式，返回每条 ScheduleRule 一条摘要，而不是展开所有未来实例。

### 流程四：修改日程

#### 4.1 修改一次性日程

根据 schedule_id 直接修改 Schedule。

#### 4.2 修改已经生成的周期实例

根据 schedule_id 修改 Schedule。

#### 4.3 修改尚未生成的周期实例

根据以下组合定位：

```text
rule_id + original_start_time
```

只创建或更新 ScheduleException。未来生成器生成该实例时应用例外。

如果调整后的 start_time 已经进入当前物化窗口，应立即创建对应 Schedule，而不是等待 original_start_time 进入窗口。

#### 4.4 修改整条周期规则

根据 rule_id 修改 ScheduleRule：

1. 保留已经发生的历史 Schedule
2. 在事务中物理删除该规则尚未发生的未来 Schedule，避免唯一键阻止重新生成
3. 删除该规则尚未发生的未来 ScheduleException；其原始时间基于旧规则，不能直接沿用
4. 重置 generated_until
5. 按新规则重新生成未来时间窗口

修改整条周期规则前需要二次确认。OperationRecord 必须保存被删除的未来实例和例外快照，以支持撤销。

### 流程五：删除日程

#### 5.1 删除一次性日程

根据 schedule_id 将该 Schedule 标记为已取消。

#### 5.2 跳过周期日程中的某一次

- 已生成：将对应 Schedule 标记为已取消
- 未生成：只保存 ScheduleException（type=skip）

恢复被跳过的单次实例时，删除对应 skip 例外；如果 original_start_time 已经早于 generated_until，则立即重新物化该实例。

#### 5.3 取消整条周期规则

1. 将 ScheduleRule.status 更新为 cancelled
2. 将未发生的未来 Schedule 标记为 cancelled
3. 保留历史实例
4. 后台生成器不再处理该规则

### 流程六：操作撤销

创建、修改、删除规则或实例后写入 OperationRecord。

撤销时根据目标类型和 previous 快照恢复：

```text
语音交互层 -> 查询最近操作服务
    -> 语音交互层 -> 撤销操作服务 -> 语音交互层
```

规则修改涉及未来实例重建时，撤销操作需要同时恢复规则和未来实例状态。

## 4. 业务接口设计

本节定义日程模块内部的应用服务接口。接口面向模块内的 Command、Result 和领域实体，不定义上层交互适配层的参数格式。

一次性日程和周期日程使用不同的创建接口：

- 一次性日程只创建 Schedule
- 周期日程先创建 ScheduleRule，再生成未来时间窗口内的 Schedule

### 4.1 接口总览

```text
CreateOneTimeSchedule        创建一次性日程
CreateRecurringSchedule      创建周期日程规则并生成实例
EnsureScheduleInstances      补充规则未来时间窗口内的实例

QueryScheduleOccurrences     查询具体日程实例
QueryScheduleRules           查询周期规则定义

UpdateScheduleOccurrence     修改一次已生成或未生成的日程实例
UpdateScheduleRule            修改整条周期规则
DeleteSchedule                删除一次性日程或已生成实例
SkipScheduleOccurrence       跳过周期日程中的某一次
CancelScheduleRule            取消整条周期规则

RecordScheduleOperation       记录日程操作
QueryRecentScheduleOperation  查询最近日程操作
UndoScheduleOperation         撤销日程操作
```

### 4.2 创建一次性日程

**接口：** CreateOneTimeSchedule

**入参：**

- **CreateOneTimeScheduleCommand：**
  - event（string，必填）：事件标题
  - start_time（DateTime，必填）：开始时间
  - end_time（DateTime，可选）：结束时间；为空表示时间点事件
  - location（optional<string>）：事件地点
  - notes（optional<string>）：事件备注
  - ignore_conflict（bool，默认 false）：是否忽略时间冲突

**处理规则：**

1. 创建一条 Schedule，rule_id 为空
2. 执行时间冲突检查
3. 成功后记录 OperationRecord

**出参：**

- **CreateOneTimeScheduleResult：**
  - status：执行状态
  - schedule（optional<Schedule>）：创建后的日程
  - conflicts（vector<Schedule>）：冲突日程
  - message：处理说明
  - error：错误信息

### 4.3 创建周期日程

**接口：** CreateRecurringSchedule

**入参：**

- **CreateRecurringScheduleCommand：**
  - event（string，必填）：周期日程默认标题
  - start_time（DateTime，必填）：首次发生的完整时间
  - end_time（DateTime，可选）：首次发生的结束时间；规则只保存本地时间部分
  - location（optional<string>）：默认地点
  - notes（optional<string>）：默认备注
  - freq_type（Frequency，必填）：daily、weekly、monthly 或 yearly
  - interval_val（integer，默认 1）：周期间隔
  - weekdays_mask（optional<int>）：每周重复的星期位图
  - day_of_month（optional<int>）：每月或每年的日期
  - month_of_year（optional<int>）：每年的月份
  - monthly_mode（optional<MonthlyMode>）：指定日期或当月最后一天
  - end_date（optional<Date>）：规则结束日期
  - occurrence_count（optional<int>）：最大重复次数
  - timezone（string，默认 Asia/Shanghai）：周期计算时区
  - ignore_conflict（bool，默认 false）：是否忽略时间冲突

**处理规则：**

1. 校验周期规则和首次发生时间
2. 创建一条 ScheduleRule
3. 生成未来配置时间窗口内的 Schedule
4. 所有实例处理成功后更新 generated_until
5. 创建规则、生成实例、更新游标和记录操作使用同一事务

**出参：**

- **CreateRecurringScheduleResult：**
  - status：执行状态
  - rule（optional<ScheduleRule>）：创建后的周期规则
  - schedules（vector<Schedule>）：本次生成的实例
  - conflicts（vector<Schedule>）：冲突日程
  - message：处理说明
  - error：错误信息

### 4.4 补充周期实例

**接口：** EnsureScheduleInstances

**入参：**

- **EnsureScheduleInstancesCommand：**
  - rule_id（ScheduleRuleId，必填）：周期规则 ID
  - window_start（DateTime，必填）：生成窗口开始
  - window_end（DateTime，必填）：生成窗口结束，采用排他上界

**处理规则：**

1. 读取 ScheduleRule
2. 从 generated_until 继续计算原始发生时间
3. 检查已存在 Schedule 和 ScheduleException
4. 应用单次例外后补充缺少的 Schedule
5. 全部成功后更新 generated_until

该接口由设备启动、恢复运行、时间同步和统一后台任务调用，不为每条规则创建独立定时器。

**出参：**

- **EnsureScheduleInstancesResult：**
  - status：执行状态
  - schedules（vector<Schedule>）：本次新增或恢复的实例
  - generated_until（DateTime）：更新后的排他上界
  - error：错误信息

### 4.5 查询具体日程实例

**接口：** QueryScheduleOccurrences

**入参：**

- **QueryScheduleOccurrencesCommand：**
  - schedule_id（optional<ScheduleId>）：按实例 ID 精确查询
  - rule_id（optional<ScheduleRuleId>）：按规则 ID 查询
  - keyword（optional<string>）：事件、地点或备注关键词
  - start_from（optional<DateTime>）：实际开始时间下限
  - start_to（optional<DateTime>）：实际开始时间上限
  - status（ScheduleStatusFilter，默认 active）：状态筛选
  - limit（integer，默认 10，最大 50）：返回条数
  - offset（integer，默认 0）：分页偏移量

**处理规则：**

- 无时间条件时，默认查询当前时间至未来 30 天
- 查询范围在物化窗口内时，确保生成后查询 Schedule
- 查询范围超出物化窗口时，合并已生成 Schedule、规则动态计算结果和 ScheduleException
- 应用例外后再按实际 start_time、keyword 和 status 过滤
- 未落库的动态实例使用 rule_id + original_start_time 定位

**出参：**

- **QueryScheduleOccurrencesResult：**
  - occurrences（vector<ScheduleOccurrence>）：具体日程实例
  - total（integer）：符合条件的总数
  - error：错误信息

ScheduleOccurrence 是查询返回对象。已生成实例包含 schedule_id；未生成实例的 schedule_id 为空，但包含 rule_id 和 original_start_time。

### 4.6 查询周期规则

**接口：** QueryScheduleRules

**入参：**

- **QueryScheduleRulesCommand：**
  - rule_id（optional<ScheduleRuleId>）：按规则 ID 查询
  - keyword（optional<string>）：标题、地点或备注关键词
  - status（ScheduleRuleStatusFilter，默认 active）：规则状态
  - limit（integer，默认 10，最大 50）：返回条数
  - offset（integer，默认 0）：分页偏移量

**处理规则：**

该接口只返回周期规则定义，不展开无限的未来实例。

**出参：**

- **QueryScheduleRulesResult：**
  - rules（vector<ScheduleRule>）：周期规则列表
  - total（integer）：符合条件的总数
  - error：错误信息

### 4.7 修改一次日程实例

**接口：** UpdateScheduleOccurrence

**入参：**

- **UpdateScheduleOccurrenceCommand：**
  - schedule_id（optional<ScheduleId>）：已生成实例的 ID
  - rule_id（optional<ScheduleRuleId>）：未生成实例的规则 ID
  - original_start_time（optional<Date>）：未生成实例的原始发生日期
  - event（optional<optional<string>>）：新标题；外层为空表示不修改，内层为空表示清空
  - start_time（optional<DateTime>）：新的开始时间
  - end_time（optional<optional<DateTime>>）：新结束时间
  - location（optional<optional<string>>）：新地点
  - notes（optional<optional<string>>）：新备注
  - ignore_conflict（bool，默认 false）：是否忽略时间冲突

**定位约束：**

- 已生成实例：必须提供 schedule_id
- 未生成实例：必须提供 rule_id + original_start_time
- 两种定位方式不能同时提供

**处理规则：**

- 一次性或已生成周期实例：更新 Schedule，并保存单次例外
- 未生成周期实例：只创建或更新 ScheduleException
- 调整后的时间进入当前物化窗口时，立即生成 Schedule
- type=skip 的例外通过删除例外记录恢复；如果已经越过 generated_until，则立即重新物化

**出参：**

- **UpdateScheduleOccurrenceResult：**
  - schedule（optional<Schedule>）：更新后的实例
  - exception（optional<ScheduleException>）：创建或更新的例外
  - conflicts（vector<Schedule>）：冲突日程
  - error：错误信息

### 4.8 修改周期规则

**接口：** UpdateScheduleRule

**入参：**

- **UpdateScheduleRuleCommand：**
  - rule_id（ScheduleRuleId，必填）：规则 ID
  - event（optional<optional<string>>）：默认标题
  - location（optional<optional<string>>）：默认地点
  - notes（optional<optional<string>>）：默认备注
  - recurrence（optional<RecurrenceRulePatch>）：周期规则局部更新
  - ignore_conflict（bool，默认 false）：是否忽略时间冲突

RecurrenceRulePatch 用于表示周期规则的局部更新，不提供的字段保持原值；使用双层 optional 的字段，外层为空表示不修改，内层为空表示清空。

```text
RecurrenceRulePatch {
    optional<Frequency>       freq_type;
    optional<int>             interval_val;
    optional<int>             weekdays_mask;
    optional<int>             day_of_month;
    optional<int>             month_of_year;
    optional<MonthlyMode>     monthly_mode;
    optional<Time>            start_time;
    optional<Time>            end_time;
    optional<optional<Date>>  end_date;
    optional<optional<int>>   occurrence_count;
    optional<Timezone>        timezone;
}
```

**处理规则：**

1. 保留历史 Schedule
2. 在同一事务中删除尚未发生的未来 Schedule 和 ScheduleException
3. 重置 generated_until
4. 根据新规则重新生成未来窗口
5. 记录包含规则、未来实例和例外快照的 OperationRecord

该操作需要二次确认。

**出参：**

- **UpdateScheduleRuleResult：**
  - rule（optional<ScheduleRule>）：更新后的规则
  - schedules（vector<Schedule>）：按新规则生成的未来实例
  - conflicts（vector<Schedule>）：冲突日程
  - error：错误信息

### 4.9 删除一次或跳过一次

**接口：** DeleteSchedule / SkipScheduleOccurrence

**DeleteSchedule 入参：**

- **DeleteScheduleCommand：**
  - schedule_id（ScheduleId，必填）：一次性日程或已生成实例 ID

**SkipScheduleOccurrence 入参：**

- **SkipScheduleOccurrenceCommand：**
  - schedule_id（optional<ScheduleId>）：已生成实例 ID
  - rule_id（optional<ScheduleRuleId>）：未生成实例的规则 ID
  - original_start_time（optional<Date>）：未生成实例的原始发生日期

**处理规则：**

- DeleteSchedule：将 Schedule.status 设置为 cancelled
- SkipScheduleOccurrence：已生成实例标记 cancelled，未生成实例创建 type=skip 的 ScheduleException
- 未生成实例必须提供 rule_id + original_start_time
- 已生成实例必须提供 schedule_id

### 4.10 取消周期规则

**接口：** CancelScheduleRule

**入参：**

- **CancelScheduleRuleCommand：**
  - rule_id（ScheduleRuleId，必填）：规则 ID

**处理规则：**

1. 将 ScheduleRule.status 设置为 cancelled
2. 将尚未发生的未来 Schedule 设置为 cancelled
3. 保留历史 Schedule
4. 后台生成器不再处理该规则
5. 记录 OperationRecord

### 4.11 操作记录、查询和撤销

**接口：** RecordScheduleOperation

该接口由创建、修改、删除等业务接口在同一事务中调用，不作为独立的上层交互接口。

**入参：**

- **RecordScheduleOperationCommand：**
  - type（ScheduleOperationType）：create、update、delete 或 undo
  - target_type（ScheduleOperationTargetType）：schedule、rule 或 exception
  - target_id（integer）：目标实体 ID
  - previous（optional<json>）：操作前快照
  - target_operation_id（optional<OperationId>）：undo 对应的原操作

**接口：** QueryRecentScheduleOperation

- **QueryRecentScheduleOperationCommand：**
  - limit（integer，默认 10）：返回数量

**出参：**

- **QueryRecentScheduleOperationResult：**
  - operations（vector<OperationRecord>）：按操作时间倒序排列
  - error：错误信息

**接口：** UndoScheduleOperation

- **UndoScheduleOperationCommand：**
  - operation_id（OperationId，必填）：要撤销的操作 ID

**处理规则：**

- 默认只允许撤销最近 15 分钟内且 undoable=1 的操作
- 规则级操作撤销时，同时恢复规则、未来 Schedule 和 ScheduleException 的组合快照
- 撤销 skip 例外时，如果原始发生时间已经早于 generated_until，则立即重新物化实例

**出参：**

- **UndoScheduleOperationResult：**
  - undone（bool）：是否成功
  - operation（optional<OperationRecord>）：撤销操作记录
  - rule（optional<ScheduleRule>）：撤销后的规则
  - schedule（optional<Schedule>）：撤销后的实例
  - error：错误信息

## 5. 数据库表结构设计

### 5.1 周期规则表（ScheduleRule）

保存周期日程定义、默认内容及实例生成进度。

| 字段名           | 类型         | 约束                            | 说明                                                     |
| ---------------- | ------------ | ------------------------------- | -------------------------------------------------------- |
| id               | integer      | PK，AutoIncrement               | 周期规则自增主键                                         |
| event            | varchar(100) | NOT NULL                        | 周期日程默认标题；生成实例时复制到 schedule.event        |
| location         | varchar(100) | NULL                            | 默认地点                                                 |
| notes            | varchar(200) | NULL                            | 默认备注                                                 |
| freq_type        | tinyint      | NOT NULL                        | 频率：1（daily）、2（weekly）、3（monthly）、4（yearly） |
| interval_val     | integer      | NOT NULL，DEFAULT 1，> 0        | 周期间隔                                                 |
| weekdays_mask    | tinyint      | NULL，1～127                    | 周规则位图：bit0=周一，bit6=周日                         |
| day_of_month     | tinyint      | NULL，1～31                     | 每月或每年的日期                                         |
| month_of_year    | tinyint      | NULL，1～12                     | 每年发生的月份                                           |
| monthly_mode     | tinyint      | NULL                            | 月模式：1（指定日期）、2（当月最后一天）                 |
| start_time       | time         | NOT NULL                        | 每次日程默认发生时间                                     |
| end_time         | time         | NULL                            | 每次日程默认结束时间                                     |
| start_date       | date         | NOT NULL                        | 规则生效日期                                             |
| end_date         | date         | NULL                            | 规则失效日期                                             |
| occurrence_count | integer      | NULL，> 0                       | 最大实例数量；与 end_date 二选一                         |
| generated_until  | datetime     | NULL                            | 已确认处理完成的原始实例排他上界                         |
| status           | tinyint      | NOT NULL，DEFAULT 1             | 规则状态：1（active）、2（cancelled）、3（completed）    |
| created_at       | datetime     | NOT NULL                        | 规则创建时间                                             |
| updated_at       | datetime     | NOT NULL                        | 最后修改时间                                             |

### 5.2 日程实例表（Schedule）

保存一次性日程以及周期规则生成的具体日程实例。

| 字段名     | 类型         | 约束                       | 说明                                              |
| ---------- | ------------ | -------------------------- | ------------------------------------------------- |
| id         | integer      | PK，AutoIncrement          | 日程实例自增主键                                  |
| rule_id    | integer      | NULL，FK → ScheduleRule.id | 来源规则；一次性日程为 NULL                       |
| event      | varchar(100) | NOT NULL                   | 实例标题；周期实例创建时复制规则默认值            |
| start_time | datetime     | NOT NULL                   | 实际开始时间，可能经过单次调整                    |
| end_time   | datetime     | NULL，> start_time         | 实际结束时间                                      |
| location   | varchar(100) | NULL                       | 实例地点                                          |
| notes      | varchar(200) | NULL                       | 实例备注                                          |
| status     | tinyint      | NOT NULL，DEFAULT 1        | 状态：1（active）、2（cancelled）、3（completed） |
| created_at | datetime     | NOT NULL                   | 实例创建时间                                      |
| updated_at | datetime     | NOT NULL                   | 最后修改时间                                      |

### 5.3 周期规则未生成且被修改的日程表（ScheduleException）

记录周期日程中某一次未生成或已生成实例的跳过和单次修改。

| 字段名              | 类型         | 约束                           | 说明                             |
| ------------------- | ------------ | ------------------------------ | -------------------------------- |
| id                  | integer      | PK，AutoIncrement              | 单次例外自增主键                 |
| rule_id             | integer      | NOT NULL，FK → ScheduleRule.id | 所属周期规则                     |
| original_start_time | date         | NOT NULL                       | 规则原本计算出的发生日期         |
| schedule_id         | integer      | NULL，FK → Schedule.id         | 对应实例；尚未生成时为 NULL      |
| type                | tinyint      | NOT NULL                       | 例外类型：1（modify）、2（skip） |
| override_start_time | datetime     | NULL                           | 调整后的开始时间                 |
| override_end_time   | datetime     | NULL                           | 调整后的结束时间                 |
| override_event      | varchar(100) | NULL                           | 调整后的标题                     |
| override_location   | varchar(100) | NULL                           | 调整后的地点                     |
| override_notes      | varchar(200) | NULL                           | 调整后的备注                     |
| created_at          | datetime     | NOT NULL                       | 例外创建时间                     |
| updated_at          | datetime     | NOT NULL                       | 最后修改时间                     |

### 5.4 操作记录表（OperationRecord）

记录日程、周期规则及单次例外的创建、修改、删除和撤销操作。

| 字段名              | 类型         | 约束                | 说明                                                       |
| ------------------- | ------------ | ------------------- | ---------------------------------------------------------- |
| id                  | integer      | PK，AutoIncrement   | 操作记录自增主键                                           |
| type                | tinyint      | NOT NULL            | 操作类型：1（create）、2（update）、3（delete）、4（undo） |
| target_type         | tinyint      | NOT NULL            | 目标类型：1（schedule）、2（rule）、3（exception）         |
| target_id           | integer      | NOT NULL            | 被操作实体的 ID                                            |
| target_event        | varchar(100) | NULL                | 操作发生时的目标标题；无标题时为空                         |
| operated_at         | datetime     | NOT NULL            | 操作发生时间                                               |
| previous            | json         | NULL                | 操作前完整快照；create 操作为 NULL                         |
| target_operation_id | integer      | NULL                | undo 操作指向被撤销的操作                                  |
| undoable            | tinyint      | NOT NULL，DEFAULT 1 | 是否仍允许撤销：0（否）、1（是）                           |

