# Proposal 03：任务自动重排

> 状态：Draft / FullSpec  
> 单一设计点：定义任务状态或日期变化后，如何依据 Proposal 02 的排序结果自动调整个人计划。  
> 依赖：Proposal 01、Proposal 02。  
> 本 Proposal 不定义新的优先级规则。

## 用户故事

我的个人任务计划形成后仍会不断变化：今天的任务没有完成、任务被阻塞、PR 下午被退回，或者硬截止发生变化。如果每次都要求我重新选择、移动和排序，产品只是把手工计划搬到了另一个界面。
面对不可避免的任务变化，我希望产品应当根据既有优先级和容量自动产生下一版计划，清楚展示哪些任务被加入、移出或改变顺序，让我能只需理解变化并继续推进。

## 目标用户

- 已经通过 Proposal 01 建立个人任务池的训练营成员；
- 已经能通过 Proposal 02 获得可解释的优先级排序；
- 不希望在任务未完成或 PR 被退回后手动维护计划。

## 现有做法及其不足

| 现有做法 | 不足 |
|---|---|
| 未完成任务继续留在原日期 | 今日列表失真 |
| 用户每天手动移动任务 | 重复维护失去便捷性 |
| 所有任务原样顺延到明天 | 不考虑明日容量，复制超载 |
| PR 被退回后靠用户自己插回计划 | 返工容易被遗漏或挤压其他任务 |
| 系统静默自动调整 | 用户不知道计划为什么变化，难以信任 |

## 本期范围与明确不做

### 本期范围

1. 使用 Proposal 02 提供的排序结果，不在本 Proposal 内重新计算优先级。
2. 使用产品侧默认每日容量；特殊日期采用用户设置的当日覆盖值。
3. 以下事件触发自动重排：
   - 日期切换时，今日任务仍未完成；
   - Issue 或 PR 状态变化导致任务完成、重新打开或被阻塞；
   - PR Review 出现 requested changes，需要返工；
   - 硬截止变化；
   - 新任务通过 GitHub Provider 进入任务池。
4. 重排时重新读取 Proposal 02 的排序结果，按顺序和预计投入填充目标日期容量。
5. 容量不足时，自动移出排序较低的任务；任务回到待规划池，不被删除。
6. 当日发生变化且仍有剩余容量时，直接更新今日计划；今日无容量时，任务进入下一日候选。
7. 跨日时，未完成且未阻塞的任务自动进入下一日候选，再按同一规则形成下一日计划。
8. 标记阻塞的任务保持阻塞，不进入可执行计划。
9. 自动重排直接生效，不要求用户确认；必须展示新增、移出、顺序变化和原因。
10. 自动重排只修改产品侧计划日期和顺序，不修改硬截止、预计投入或 GitHub 状态。
11. 如果排序更高的硬截止任务总投入已经超过容量，保留冲突并展示，不通过静默延期制造可完成假象。
12. 缺少预计投入的任务不能参与容量填充，显示“待估算”和对应风险。
13. 已消耗容量统一按任务的原预计投入计算，不要求用户记录实际投入，也不使用 Commit、在线时长等代理值。
14. 用户手动调整自动重排结果后，该顺序保存为同一硬截止分组内的稳定顺序，供 Proposal 02 和后续重排继续使用。

### 本期明确不做

- 在本 Proposal 内定义用户优先级算法；
- 未经 Proposal 02 排序直接决定任务重要性；
- 修改 GitHub Issue、PR 或硬截止；
- 删除因容量不足被移出的任务；
- 自动缩短预计投入；
- 自动拆分 Issue；
- 自动安排到后天或更远日期的长期计划；
- 团队成员任务调整；
- 多仓库或跨项目资源调度；
- 要求用户逐项确认自动变化。

## 关键决策与依据

### 决策 1：重排只消费排序结果

| 方案 | 判断 |
|---|---|
| Proposal 03 再定义一套排序 | 容易与 Proposal 02 冲突，两个页面可能给出不同结论 |
| Proposal 03 只使用 Proposal 02 输出 | 排序规则唯一，可以分别验证优先级和计划变化 |

**选择**：Proposal 02 决定谁更优先，Proposal 03 决定在容量和事件变化下谁进入实际计划。

### 决策 2：自动执行，用户只看变化

自动重排的价值是减少手工计划负担，因此不要求用户逐项确认。系统必须提供变化说明，并保留用户事后手动调整的能力。

### 决策 3：移出计划不等于删除任务

容量不足时，较低排序任务只离开当前日期计划并回到待规划池。Provider 对象、硬截止和任务内容保持不变。

### 决策 4：当日重排与跨日重排使用同一机制

二者区别只在触发事件和目标日期：

- 当日重排：PR 被退回、任务阻塞或新任务进入后，重新填充今天的剩余容量；
- 跨日重排：今日未完成任务进入下一日候选，再填充下一日容量。

两者都复用 Proposal 02 的排序结果，不维护两套算法。

### 决策 5：重排事件实时生效

GitHub Provider 或产品侧识别到未完成、阻塞、requested changes、硬截止变化或新任务后，立即触发重排，不等待定时批处理或用户重新打开页面。具体传输技术属于交付设计，本 Proposal 只规定用户应看到实时变化。

### 决策 6：已消耗容量使用原预计投入

任务完成后，已消耗容量直接使用该任务原预计投入。MVP 不要求用户记录实际投入，也不通过 Commit、开始结束时间或在线状态推测实际工时。这可能与真实耗时存在偏差，但规则简单、稳定且无额外操作。

```text
目标日期剩余容量
= 每日容量
- 已完成任务的原预计投入
- 当前继续保留任务的预计投入
```

### 决策 7：手动调整形成稳定顺序

用户手动调整自动结果后，产品保存该顺序。后续重排在同一硬截止分组内继续遵守手动顺序；如果硬截止分组发生变化，Proposal 02 可以覆盖该顺序，并由 Proposal 03 展示变化原因。


## 基本概念与信息结构

- **重排事件**：足以让当前计划失真的 Provider 或产品侧状态变化。
- **当前计划**：某个日期已经生效的个人任务集合与顺序。
- **目标日期**：本次重排需要更新的日期，可能是今天或下一天。
- **每日容量**：目标日期可用于当前项目的预计时间。
- **已消耗容量**：已完成任务原预计投入的总和。
- **剩余容量**：目标日期容量扣除已完成任务原预计投入和继续保留任务预计投入后的可用量。
- **待规划池**：未进入目标日期计划、等待后续事件再次参与重排的任务。
- **变化记录**：新增、移出、顺序调整及其触发原因。
- **稳定手动顺序**：用户调整后保存在产品侧、对同一硬截止分组后续重排生效的顺序。

```text
重排事件 + 当前计划 + 每日容量
              ↓
       读取 Proposal 02 排序结果
              ↓
          按容量填充
          ├─ 纳入目标日期
          └─ 超出容量 → 待规划池
              ↓
        新计划 + 变化记录
```

## 原型 / Demo

至少包含：

1. 自动重排后的个人任务计划；
2. 新增、移出和顺序变化的对比；
3. 容量不足与硬截止冲突；
4. 待估算、阻塞和待规划池状态。

**例子一：当日 PR 被退回**

- 上午：任务 A 提交 PR，进入待 Review。
- 下午：PR 收到 requested changes，任务 A 重新进入任务池。
- Proposal 02：根据硬截止给出新的排序结果。
- Proposal 03：今天仍有 1 小时容量，自动把 A 加回今日计划，并移出排序更低的任务 C。
- 用户看到：“A 因 PR 被退回加入今天；C 因容量不足被移出。”

**例子二：跨日未完成**

- 今天结束时 B、C 未完成，明天默认容量为 1 小时。
- Proposal 02：B 的硬截止早于 C，因此 B 排在前面。
- Proposal 03：自动把 B 放入明日计划，将 C 放回待规划池。
- 用户次日直接看到新的计划和变化，无需先操作。

**边界与非法情况**：

- 如果硬截止任务本身已经超过容量，显示硬冲突，不修改硬截止。
- 如果任务没有预计投入，显示待估算，不虚构数值参与容量填充。
- 系统不能因为移出今日计划而关闭或删除 GitHub Issue。

原型链接：`待补充`

## 验收标准

1. 所有重排结果都基于 Proposal 02 的排序输出。
2. 日期切换、任务阻塞、PR 被退回和新任务进入能够触发重排。
3. 当日有剩余容量时，重新进入的高排序任务能够自动加入今日计划。
4. 容量不足时，排序较低任务自动离开目标日期并进入待规划池。
5. 今日未完成任务能够自动参与下一日重排。
6. 自动重排不需要用户确认，但每项变化都有可见原因。
7. 自动变化不修改硬截止、预计投入、GitHub 状态或任务内容。
8. 阻塞任务不进入可执行计划。
9. 缺少预计投入的任务不会获得虚构值。
10. 硬截止任务超过容量时显示冲突，不静默延期。
11. Demo 能分别跑通一次当日重排和一次跨日重排。
12. 重排事件被识别后实时更新计划和变化说明。
13. 已完成任务统一按原预计投入扣减容量，不要求填写实际投入。
14. 用户手动调整后的同组顺序在下一次重排中保持稳定。

## 待评审问题

当前产品行为问题均已形成初步决策；具体实时同步技术在交付架构设计阶段确定。
