# Proposal 03：任务自动重排

> 状态：Draft / FullSpec  
> 单一设计点：当个人计划因任务未完成、被阻塞或 PR 被退回而发生变化时，让用户无需重新排期，直接看到新的可执行计划和变化原因。  
> 依赖：Proposal 01 提供个人任务池，Proposal 02 提供任务优先级。  
> 本 Proposal 不重新定义任务优先级。

## 动机 / 用户故事

作为一名推进个人项目的 X Engineer 训练营成员，我每天会先查看自己要完成的任务，但实际执行经常与早上的计划不同：

- 今天的任务没有按时完成；
- 正在做的任务被阻塞；
- 上午提交的 PR 下午收到 `changes requested`；
- 新任务进入仓库；
- 原有任务的硬截止发生变化。

现在遇到这些情况时，我需要重新检查所有任务、判断优先级，再手动移动任务。如果每天都要重复这套操作，任务管理工具没有真正减少我的负担。

我希望任务发生变化后，不需要重新填写和确认计划，就能立即看到调整后的今日或明日任务，并清楚知道哪些任务被加入、移出或提前，以及为什么发生变化。

**共同需求一句话提炼**：计划发生变化时，用户无需重新排期，只需查看新的任务安排和变化原因，然后继续执行。

## 目标用户

- X Engineer 训练营成员；
- 已从一个 GitHub Repository 导入个人相关 Issue 和 PR；
- 已经可以看到 Proposal 02 产生的任务优先级；
- 需要管理自己的每日任务，不负责安排其他成员的工作；
- 不希望因任务状态变化而反复手动维护计划。

## 现有做法及其不足

| 用户当前怎么做 | 仍然存在的问题 |
|---|---|
| 未完成任务继续留在“今天” | 今日列表逐渐失真，不知道哪些仍是今天真正要做的 |
| 第二天手动把任务逐项移过去 | 每天重复整理，容易漏掉任务 |
| 把所有未完成任务都放到明天 | 明日任务可能超过可用时间 |
| PR 被退回后自己插回今日列表 | 容易忘记返工，也可能挤占更紧急任务 |
| 工具直接改变计划但不解释 | 用户不知道为什么变化，难以信任结果 |

## 本期范围与明确不做

### 本期范围

#### 1. 用户无需主动发起重排

用户不需要点击“重新规划”或刷新页面。出现以下可被用户感知的情况时，当前任务列表自动更新：

- 新 Issue 或新的 PR Review 请求进入我的任务；
- PR 提交或完成返工后重新请求 Review；
- PR 收到 `changes requested`，或出现明确需要作者修改的未解决 Review 意见；
- Issue 被重新打开；
- 用户在产品中标记阻塞，或 GitHub Issue 出现约定的 `blocked` label；
- 任务硬截止发生变化；
- Issue 关闭，或 PR 合并、关闭，任务不再需要继续执行；
- 日期切换后，昨天仍有任务未完成。

#### 2. 三个主状态决定任务是否参与重排

- **On Progress（进行中）**：当前需要用户采取行动，任务参与优先级排序和自动重排。
- **Pending Review（待评审）**：用户已提交或完成修改，正在等待 Review 或合并；不作为待执行任务参与重排。
- **Done（已结束）**：对用户来说当前已无待处理动作，任务退出可执行任务池与重排。

用户提交 PR，或完成返工并重新请求 Review 后，任务从 `On Progress` 进入 `Pending Review`。收到 `changes requested` 或明确需要作者修改的未解决 Review 意见时，任务回到 `On Progress`。

`Approved` 或不要求作者修改的普通评论不改变主状态，任务保持 `Pending Review`。PR 合并或关闭后进入 `Done`；页面分别显示“已合并”或“已关闭未合并”。任务后续被 Reopen 时回到 `On Progress`，并保留原有优先级信息、硬截止、预计投入和手动顺序，重新参与排序。

#### 3. 用户直接看到调整后的计划

重排完成后，用户看到新的今日或明日任务列表，不需要逐项确认。页面同时展示变化摘要：

- 哪些任务被加入；
- 哪些任务被移出；
- 哪些任务顺序发生变化；
- 每项变化的原因。

#### 4. 今日容量不足时自动移出较低优先任务

当新的高优先任务进入今日计划，而今日可用容量不足时，较低优先任务自动离开今日计划并回到“规划池”。

任务只是离开今天，不会被删除，也不会被自动安排到后天或更远日期。

#### 5. 今日无可用容量时进入规划池

今日容量不足时，较低优先任务只回到规划池，不立即安排到明天。日期切换或用户查看下一日计划时，规划池任务再与下一日候选任务一起参与优先级排序和容量安排。

#### 6. 被阻塞的任务不进入可执行计划

用户将任务标记为阻塞，或 GitHub 状态表明任务暂时无法继续时，该任务离开今日执行列表，并在阻塞区域保留。解除阻塞后，它再次参与任务安排。

#### 7. 用户不需要记录实际工时

任务完成后，今日容量按照任务原来的预计投入扣减。用户不需要补填实际投入，产品也不根据 Commit、在线时间或开始结束时间猜测工时。

#### 8. 用户手动顺序会被保留

用户可以在自动重排后手动调整任务顺序。对于硬截止相同的任务，后续刷新和重排继续保持这个手动顺序。

如果某项任务出现更早的硬截止，它可以被自动提前；用户会看到“因硬截止提前”的说明。

#### 9. 时间信息不足时明确展示

缺少预计投入的任务仍可参与优先级展示，但不能进入今日计划。任务进入“待估算”区域；用户补充预计投入后，任务才参与容量分配和自动重排。

#### 10. 硬截止任务超过容量时由用户决定

如果仅必须今日完成的硬截止任务就已超过今日剩余容量，用户会看到：

- 哪些任务发生冲突；
- 当前可用时间、所需时间和缺口；
- 可选处理方向：调高今日容量、缩小任务范围或预计投入、手动移出某个任务，或明确接受可能延期。

在用户选择前，页面保留“硬截止容量冲突”提示。产品不静默延期，也不替用户承诺一个无法完成的计划。

### 本期明确不做

- 让用户逐项确认自动变化；
- 在本 Proposal 中重新定义任务优先级；
- 在 App 中修改 GitHub Issue、PR 或硬截止；
- 删除被移出今日计划的任务；
- 自动缩短任务预计投入；
- 自动拆分 Issue；
- 自动规划后天或更远日期；
- 安排或调整其他成员的任务；
- 多仓库和跨项目资源调度；
- 记录或推测用户实际工时。

## 关键决策与依据

### 决策 1：用户看到的是自动完成的重排，不是重排建议

| 方案 | 用户体验 | 判断 |
|---|---|---|
| 每次变化后要求用户重新确认 | 用户拥有完全控制权 | 每次任务变化都增加操作，无法减少计划负担 |
| 自动调整并展示变化 | 用户直接继续执行，必要时再手动调整 | 操作最少，同时保留理解和修改能力 |

**选择**：在可产生可执行计划时，自动重排直接生效，用户只需查看变化。唯一例外是硬截止任务本身已超过容量：此时不存在不违背硬截止的自动结果，必须由用户选择处理方向。

### 决策 2：排序规则保持一致

用户在 Proposal 02 中看到什么任务更优先，自动重排就沿用相同顺序。本 Proposal 不提供另一套排序逻辑，避免用户在“优先级页面”和“今日计划”中看到互相矛盾的结果。

### 决策 3：移出今日不等于删除任务

容量不足时，较低优先任务回到规划池。用户之后仍能找到它，它的 GitHub 链接、硬截止和任务内容都不会改变。

### 决策 4：当日变化与跨日未完成采用相同体验

- 当日变化：例如 PR 下午被退回，用户立即看到今天的任务重新排列；
- 跨日变化：例如任务晚上仍未完成，用户第二天直接看到新的安排。

无论哪种情况，用户看到的都是“新计划 + 变化原因”，不需要学习两种操作方式。

### 决策 5：变化实时展示

当 GitHub 或产品中的任务状态发生变化后，用户无需手动刷新页面。当前任务列表和变化说明随之更新。Webhook、轮询等同步技术不属于本 Proposal 的产品决策。

### 决策 6：容量按照原预计投入扣减

用户完成一项原预计投入为 2 小时的任务后，产品按 2 小时计入今日已使用容量。即使实际耗时不同，MVP 也不要求用户补录。

这个选择牺牲部分精确度，换取更少的操作和稳定、容易理解的规则。

### 决策 7：手动顺序在同一硬截止分组内持续生效

如果两个任务硬截止相同，用户手动将 B 放在 A 前面，后续重排继续保持 `B → A`。如果 A 后来变为更早到期，A 可以自动提前，但页面必须解释这次变化。

### 决策 8：主状态只表达用户是否需要行动

| 方案 | 用户体验 | 判断 |
|---|---|---|
| 为 GitHub 的每种状态和原因创建主状态 | 状态多，用户需要理解 GitHub 细节 | 不利于快速判断现在是否要行动 |
| 使用三个主状态，具体情况用原因标签表达 | 用户只需判断“要做、等待、结束” | 主状态稳定，也能保留详细原因 |

**选择**：主状态只使用 `On Progress`、`Pending Review` 和 `Done`。`blocked`、“待估算”、`changes requested`、“已合并”和“已关闭未合并”均不新增主状态，只作为状态原因或展示标签。

## 基本概念与信息结构

### 用户会看到的核心对象

- **今日计划**：用户今天可以直接执行的任务列表。
- **下一日计划**：日期切换后自动形成的下一天任务列表。
- **规划池**：暂时没有进入某一天计划、但仍需后续处理的任务。
- **阻塞任务**：目前无法继续、不进入可执行计划的任务。
- **每日容量**：用户当天预计可以投入到项目的总时间。
- **剩余容量**：扣除已完成任务原预计投入和当前保留任务预计投入后的可安排时间。
- **待估算**：缺少预计投入、暂时不能参与容量安排的任务。
- **待评审**：主状态为 `Pending Review`、当前正在等待 Review 或合并的任务。
- **变化摘要**：告诉用户哪些任务被加入、移出或调整，以及对应原因。

### 三层信息结构

| 层级 | 字段 | 可选值示例 | 用途 |
|---|---|---|---|
| 主状态 | `work_status` | `on_progress` / `pending_review` / `done` | 表达用户现在是否需要行动，决定是否参与重排 |
| 状态原因 | `status_reason` | `changes_requested` / `blocked` / `missing_estimate` / `merged` / `closed_unmerged` | 补充当前状态或计划位置的原因，不单独成为主状态 |
| 计划位置 | `plan_bucket` | `today` / `planning_pool` / `pending_review` / `blocked` / `unestimated` | 表达任务当前在页面的展示位置，不是生命周期状态 |

### 用户使用路径

```text
用户查看今日计划
       ↓
在 GitHub 中推进任务
       ↓
提交 PR / Review 反馈 / 任务阻塞 / 对象关闭或 Reopen / 截止变化
       ↓
主状态是什么？
  ├─ Pending Review → 离开可执行计划，进入“待评审”区域
  ├─ Done → 退出可执行任务池，显示结束原因
  └─ On Progress
       ↓
任务信息是否足以安排？
  ├─ 缺预计投入 → 进入“待估算”，等待用户补充
  └─ 信息完整
       ↓
按 Proposal 02 的优先级和容量形成新计划
       ↓
是否存在硬截止容量冲突？
  ├─ 否 → 计划自动更新，用户查看变化后继续执行
  └─ 是 → 展示冲突、时间缺口和处理方向，由用户决定
```

## 原型 / Demo

### 原型流程

```text
查看今日计划
      ↓
提交 PR → Pending Review（待评审）
      ↓
收到 changes requested 或明确的未解决修改意见
      ↓
On Progress（进行中）并查看变化摘要
      ↓
被移出的任务回到规划池
      ↓
若不能自动形成可执行计划
  ├─ 硬截止超载 → 用户选择处理方向
  └─ 缺少预计投入 / 任务阻塞 → 进入对应区域

完成返工并重新请求 Review → Pending Review
PR 合并 / 关闭 → Done（已结束）
Issue 或未合并 PR Reopen → On Progress，重新参与排序
```

### 状态展示原则

| 用户看到的主状态 | 表示什么 | 界面行为 |
|---|---|---|
| `On Progress` 进行中 | 当前需要用户行动 | 可进入今日计划或规划池 |
| `Pending Review` 待评审 | 用户已提交或完成修改，正在等待 Review 或合并 | 离开可执行计划，在待评审区域展示 |
| `Done` 已结束 | 当前无需用户继续处理 | 不参与重排，使用“已合并”或“已关闭未合并”说明结果 |

### 界面 1：查看今日计划

```text
今日计划                                  7 月 14 日
已安排 4h / 今日容量 4h

1. Issue #31  完成登录页              2h  截止：今天
2. Issue #37  补充错误提示          2h  截止：7 月 16 日

用户可以直接执行排在最前的任务；
硬截止相同时，可拖动调整顺序。
```

### 界面 2：提交 PR 后进入待评审

```text
待评审
Issue #31 / PR #45  完成登录页
状态：Pending Review
原因：已提交 PR，等待 Reviewer

当前不需要用户修改，任务不参与可执行计划的重排。
Approved 或无需修改的普通评论不会改变该状态。
```

### 界面 3：PR 被要求修改后查看变化

```text
计划已更新
+ Issue #31 从 Pending Review 回到 On Progress
+ 原因：PR 收到 changes requested，需返工 1h
- Issue #37 因今日容量不足，已移回规划池

今日计划已立即更新，无需用户确认。
[查看规划池]
```

### 界面 4：查看回到规划池的任务

```text
规划池
Issue #37  补充错误提示           2h
原因：今日容量不足
后续：日期切换或查看下一日计划时，
      它将与其他候选任务一起重新参与安排。
```

### 界面 5：处理硬截止容量冲突

```text
硬截止容量冲突
今日剩余容量：2h  必须今日完成：3h  缺口：1h

Issue #31  PR 返工                       1h
Issue #42  提交训练营作业             2h

请选择处理方向：
[调高今日容量] [调整任务范围/预计投入]
[手动移出任务] [接受可能延期]

用户选择前，产品不自动修改硬截止，也不隐藏冲突。
```

### 界面 6：处理待估算与阻塞任务

```text
待估算
Issue #51  更新项目文档          [补充预计投入]
说明：已展示优先级，但补充时间前不进入今日计划。

阻塞任务
Issue #18  等待 Reviewer 回复        [标记为已解除]
说明：阻塞期间不占用今日容量。

补充预计投入或解除阻塞后，任务自动重新参与安排。
```

### 界面 7：查看结束结果与 Reopen 变化

```text
已结束
PR #45  完成登录页               [已合并]
PR #52  尝试旧的数据方案         [已关闭未合并]

两个对象的主状态都是 Done，但用标签区分结果。

如果 Issue 或未合并 PR 后续被 Reopen：
+ 主状态：Done → On Progress
+ 保留：原优先级信息、硬截止、预计投入和手动顺序
+ 后续：按当前计划重新参与排序和重排
```

### 行为示例

#### 示例一：PR 下午被退回

**现状**

小林上午提交任务 A 的 PR，任务从 `On Progress` 进入 `Pending Review`。下午 PR 收到 `changes requested`，返工预计需要 1 小时。小林今天还剩 1 小时可用容量。

**提议后的体验**

小林不需要重新打开规划页面或手动插入任务。任务 A 自动回到 `On Progress`，按原有优先级信息重新参与排序，今日列表随之加入 A，并移出一个截止更晚的任务 C。

页面展示：

> A 因 PR 收到 `changes requested` 从待评审回到进行中，并加入今日计划；C 因今日容量不足移回规划池。

#### 示例二：今日任务未完成

**现状**

今天结束时，任务 B 和 C 都没有完成。明天默认容量只有 1 小时，B 的硬截止早于 C。

**提议后的体验**

第二天用户打开产品时，B 已经进入今日计划，C 位于规划池。

页面展示：

> B 因硬截止更早进入今日计划；C 因容量不足暂留规划池。

#### 示例三：用户手动调整后出现更早截止

**现状**

任务 D、E 原本硬截止相同，用户手动将 E 放在 D 前面。之后 D 的硬截止提前到今天。

**提议后的体验**

任务 D 自动移动到 E 前面，同时页面展示：

> D 因硬截止变为今天而提前，覆盖原手动顺序。

### 边界与非法情况

- `Approved` 或不要求作者修改的普通评论不能让任务从 `Pending Review` 回到 `On Progress`。
- PR 关闭未合并时可以进入 `Done`，但必须显示“已关闭未合并”，不能显示为“已合并”。
- Issue 或未合并 PR Reopen 后必须回到 `On Progress`，保留原有计划信息并重新参与排序。
- 如果仅硬截止任务就已经超过今日容量，用户看到“硬截止容量冲突”；产品不能静默延期。
- 如果任务没有预计投入，用户看到“待估算”；产品不能虚构一个数值参与安排。
- 任务离开今日计划后仍在规划池；产品不能关闭或删除对应 GitHub Issue。
- 用户手动顺序只能在相同硬截止分组内保持，不能覆盖更早的硬截止。

### 可点击原型

原型链接：`待补充`


## 验收标准

| 场景 | 用户操作 | 用户应看到的结果 |
|---|---|---|
| 今日任务未完成 | 用户无需操作 | 下一日计划自动更新，并说明任务如何顺延 |
| 用户提交 PR | 用户等待 Review | 任务从 `On Progress` 进入 `Pending Review`，离开可执行计划，但仍可在“待评审”区域找到 |
| PR 收到 `approved` 或无需修改的普通评论 | 用户无需操作 | 任务保持 `Pending Review`，不重新参与排序 |
| PR 收到 `changes requested` 或明确的未解决修改意见 | 用户继续停留或再次查看任务页 | 任务回到 `On Progress`，实时参与优先级排序和重排，变化原因可见 |
| 用户完成返工并重新请求 Review | 用户提交修改 | 任务从 `On Progress` 回到 `Pending Review` |
| PR 合并或关闭 | 用户无需操作 | 任务进入 `Done` 并退出重排；分别显示“已合并”或“已关闭未合并” |
| Issue 或未合并 PR 被 Reopen | 用户无需重新录入计划信息 | 任务从 `Done` 回到 `On Progress`，保留原有优先级信息、硬截止、预计投入和手动顺序，重新参与排序 |
| 任务被阻塞 | 用户标记阻塞或 GitHub 状态变化 | 任务离开可执行计划，但仍可在阻塞区域找到 |
| 新任务进入仓库 | 用户无需手动加入 | 新任务根据既有优先级参与重排 |
| 今日容量不足 | 用户无需手动移除任务 | 较低优先任务回到规划池，并说明容量原因 |
| 硬截止任务超过容量 | 用户查看冲突并选择处理方向 | 页面展示冲突任务、所需时间和缺口；提供调高容量、调整任务、手动移出或接受延期的处理方向，不静默延期 |
| 任务缺少预计投入 | 用户查看任务 | 任务显示优先级和“待估算”状态，但不进入今日计划；补充预计投入后才参与容量安排 |
| 用户调整同组顺序 | 用户拖动任务 | 后续重排保持手动顺序 |
| 某任务硬截止提前 | 用户无需调整 | 任务可覆盖手动顺序并提前，同时展示原因 |
| 任务完成 | 用户不填写实际工时 | 今日容量按原预计投入自动扣减 |

本 Proposal 通过的最低条件：

1. 用户可以在不手动重新规划的情况下，分别完成一次当日重排和一次跨日重排体验。
2. 每项自动变化都有用户能理解的原因。
3. 自动重排不会修改 GitHub 状态、硬截止、预计投入或任务内容。
4. 从今日计划移出的任务仍可在规划池找到。
5. 用户不需要记录实际投入，也不需要逐项确认重排结果。
6. 用户可以完整看到 `On Progress → Pending Review → On Progress → Pending Review → Done` 的 PR 状态流转，并在 Reopen 后看到任务重新参与排序。

## 待评审问题

当前产品行为已经形成初步决策。实时同步采用 Webhook、轮询还是组合方式，留待交付架构设计阶段确定。
