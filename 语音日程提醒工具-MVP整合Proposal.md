# 语音日程提醒工具 MVP Proposal

本文最终确定一个产品方向：做语音优先的个人日程提醒工具。用户用语音创建、查询和处理日程；系统用语音完成当下反馈，用 IM 留下完整回执和复杂选择。

本期产品闭环是：语音创建 / 查询 -> 事件触发提醒 -> 语音即时处理 -> 日程变更 / 撤销。实现、原型和验收都按这条闭环推进。

## 1. Why：为什么要做

用户不是缺一个新的日历页面，而是在三个时刻被现有工具卡住：

1. **刚想到安排时**：打开日历、选日期、填标题、补地点，步骤太多。很多小安排就是在这几步里丢掉的。
2. **提醒响起时**：用户通常只想马上处理当前提醒，比如“知道了”“十分钟后再提醒”。如果还要进 App 找那条日程，提醒已经打断了工作。
3. **安排临时变化时**：语音说“把今天的日报改到晚上七点”很自然，但系统如果听错范围，可能把以后每天的日报都改掉。用户真正怕的是这种误改。

所以本期要验证的不是“AI 能不能管理全部生活”，而是一个更窄的判断：

> 对有零散事项和周期提醒的人来说，语音能不能把“记下来、提醒到、改得稳”这条路径做得比手动日历更省心。

## 2. 目标用户

本期只服务个人用户，不做团队日历。

目标用户有这些特征：

- 经常临时产生会议、拜访、缴费、取快递、回电话、提交周报这类事项；
- 有每天、每周、每月重复的提醒；
- 经常需要只改本次、跳过一次、稍后提醒或删除某条日程；
- 愿意用语音说出安排，也接受 IM 回执作为事后核对；
- 需要的是个人日程提醒，不是项目排期、共享日历或长期个人记忆库。

典型故事：

- 路上想起明天 10 点客户拜访，直接说出来，系统记成日程并留下地点。
- 14:30 提醒响起，用户正在开会，只说“十分钟后提醒我”，系统只推迟这次提醒，不改原日程。
- 今天日报要晚点写，用户说“把今天的日报改到晚上七点”，系统只改今天这一次，明天仍按原周期提醒。

## 3. 现有做法为什么不够

| 现有做法 | 能解决什么 | 不够在哪里 |
| --- | --- | --- |
| 手动日历 App | 结果清楚、字段完整 | 临时录入成本高，用户要停下当前动作 |
| 系统语音助手 | 快速新增提醒 | 周期范围、冲突和变更回执不清楚 |
| 备忘录 / 发给自己 | 能先记住 | 没有稳定提醒，也不容易按时间查询 |
| 闹钟 / 普通提醒 | 能打断用户 | 只知道响了，不知道后续是关闭、推迟还是改日程 |
| 删除后重建 | 勉强能改时间 | 容易丢地点、备注、周期规则和原提醒 |

本产品只解决这些不足里的核心部分：用语音完成日程类操作，同时让每次操作有可回看的 IM 结果。

## 4. What：本期做什么

本期做四件事。

| 能力 | 用户怎么说 | 系统怎么做 |
| --- | --- | --- |
| 创建 | “明天上午十点客户拜访，A 座 1503，大概两个小时” | 补齐标题、时间、地点、时长，检查冲突后创建 |
| 查询 | “我今天还有什么事”“下周有哪些安排” | 语音播摘要，IM 发完整列表 |
| 提醒处理 | “知道了”“十分钟后提醒我” | 关闭或推迟当前提醒，不改底层日程 |
| 日程变更 | “把今天的日报改到晚上七点”“这周周报跳过一次”“删掉下午三点的会” | 修改、跳过、删除或撤销，并说明影响范围 |

### 4.1 语音创建

日程至少需要标题和发生时间。

- 会议、拜访、课程这类占用一段时间的日程，必须有结束时间或持续时长。缺少时，系统追问。
- 吃药、取快递、回电话这类点状提醒，只需要触发时间。
- 周期事项只支持每天、每周、每月。
- 没有时间的小事只做 24 小时临时语音便签，例如“帮我记一下车停在 B2 蓝区”。用户可以问“我刚才让你记的事是什么”。24 小时后自动过期，不进入长期记忆，也不支持跨天模糊检索。

创建前先做冲突检查。任意两个时间段只要发生重叠，就算冲突；首尾相接不算冲突，例如 10:00-11:00 和 11:00-12:00 可以并存。点状提醒落在已有时间段内，或两个点状提醒在同一时间，也算冲突。

发现冲突时，系统先说明冲突对象，再问用户是否仍然创建。用户拒绝时，不留下新日程、新提醒或创建回执。用户确认创建时，系统保留两条日程，并在 IM 回执里标出冲突。

### 4.2 语音查询

用户可以查询今天、明天、某一天、本周、某一周、某一月、今年、下一条日程和关键词相关日程。

语音只播摘要：数量、最近一条和必要提醒。完整结果走 IM。IM 每页最多 20 条，超过 20 条提供下一页入口，避免一次消息太长。

### 4.3 事件触发提醒

这里的“事件触发”指系统认为某条提醒应该送达的触发点。MVP 只实现三类触发：

- 用户指定的主提醒时间到达；
- 有明确开始时间的日程，在开始前 15 分钟触发一条弱提醒；
- 用户选择“稍后提醒”后，新的提醒时间到达。

本期不做位置、传感器、外部消息监控等事件触发。

提醒触发时，语音和 IM 同时发送，互不阻塞。语音默认只播标题和时间，例如“提醒：14:30 客户拜访”。地点、备注、完整内容和操作入口放在 IM。用户说“详细说说”时，语音再补充地点和备注。

### 4.4 提醒即时处理

提醒即时处理只处理“当前提醒”，不修改日程本身。

用户可以：

- 说“知道了”“不用再响了”关闭当前提醒；
- 说“十分钟后提醒我”推迟当前提醒；
- 在当前 IM 回执里点关闭或选择推迟时长。

推迟规则：

- 用户没说清推迟多久时，系统追问“多久后再提醒？”；
- 用户设置过默认推迟时长时，直接使用默认值；
- 同一提醒实例最多推迟 3 次；
- 第 3 次推迟时，系统明确说“这是本次最后一次稍后提醒”；
- 第 3 次推迟后的提醒再次触发时，如果用户还要推迟，系统不再创建新的稍后提醒，只让用户选择关闭提醒或修改日程。

多条提醒同时到达时，系统按顺序播报。用户只说“知道了”但目标不清，系统先列出标题让用户选，不默认关闭任何一条。

### 4.5 已有日程变更

以下操作会改变日程本身，必须进入变更流程：

- 修改单次日程的标题、时间、地点、备注；
- 修改周期事项的仅本次、本次及以后或整个系列；
- 跳过某一次周期事项；
- 暂停周期事项到一个明确日期，到期后恢复；
- 终止周期事项的后续提醒；
- 删除单次日程或整个周期系列；
- 撤销最近一次删除或变更。

安全规则：

- 目标不唯一时不执行，先列候选项；
- 周期范围不清时不执行，先让用户选择“仅本次 / 本次及以后 / 整个系列”；
- 删除、终止、把单次事项“跳过”等同取消这些高风险操作，执行前必须确认；
- 修改后的时间发生冲突时，只提示冲突，不自动移动其他日程；
- 重复跳过同一个周期实例时，只回复“本次已经跳过”，不重复生成变更；
- 删除或变更完成后的 10 分钟内，用户可以说“撤销刚才操作”，系统恢复最近一次变更前的状态。

## 5. 明确不做

本期不做：

- 完整日历 GUI、拖拽排程、多视图日历；
- 完整 RRULE、工作日组合、法定节假日、农历、跨时区；
- 第三方日历同步、共享日历、邀请他人、修改别人的日程；
- 全天候录音、唤醒词监听、被动捕获；
- 停车位置、密码、取件码等长期个人记忆库；
- 位置、传感器、外部消息等自动触发；
- 批量删除、清空整月安排等批量高风险操作；
- 医疗依从性、病情管理或照护判断；
- 自动移动其他日程、自动替用户解决冲突、黑盒 AI 排程；
- 数据库字段、接口协议、语音识别模型等技术实现方案。

## 6. 关键决策与依据

| 决策点 | 备选方案 | 本期选择 | 理由 |
| --- | --- | --- | --- |
| 产品边界 | 完整日历 / 个人记忆库 / 日程提醒工具 | 日程提醒工具 | 创建、提醒、处理、变更是一条能在 MVP 内跑通的闭环 |
| 入口形态 | 表单为主 / 文字为主 / 语音为主 | 语音为主，IM 辅助 | 语音负责当下操作，IM 负责完整记录和复杂选择 |
| 小事记录 | 不做 / 长期记忆 / 24 小时临时便签 | 24 小时临时便签 | 接住“帮我记一下”的真实表达，但不扩成个人记忆库 |
| 周期规则 | 不做周期 / 完整 RRULE / 简单周期 | 每天、每周、每月 | 覆盖高频表达，降低规则复杂度 |
| 冲突处理 | 直接拒绝 / 自动改时间 / 提示后由用户决定 | 提示后由用户决定 | 用户可能有意保留重叠安排，系统不替用户取舍 |
| 提醒响应 | 关闭、稍后、改时间都混在一起 / 只处理提醒 | 只处理关闭和稍后 | “稍后”只改提醒状态；改日程进入变更流程 |
| 风险控制 | 所有操作都确认 / 所有操作都直接执行 / 按风险分层 | 按风险分层 | 低风险保持语音效率，高风险防误删、误改、误停周期 |
| 撤销 | 不支持 / 长期版本管理 / 10 分钟内撤销 | 10 分钟内撤销最近一次变更 | 降低语音误听成本，同时不引入复杂版本系统 |

## 7. How：产品怎么运转

### 7.1 语音和 IM 的分工

| 通道 | 负责 | 不负责 |
| --- | --- | --- |
| 语音 | 发起操作、补齐缺失信息、播报短反馈、处理当前提醒 | 长列表、复杂候选、完整变更记录 |
| IM | 创建回执、提醒详情、候选项选择、分页列表、撤销入口 | 开放式闲聊、替代完整日历管理 |

语音说短句，IM 留证据。用户听到结果，也能回头看到系统到底记了什么、改了什么、影响哪一次。

### 7.2 主流程

```text
用户说出安排
  -> 系统解析标题、时间、地点、周期
  -> 信息不够时追问
  -> 创建前检查冲突
  -> 语音反馈结果，IM 留回执
  -> 到触发事件时语音 + IM 提醒
  -> 用户关闭或稍后
  -> 如果用户要改日程，进入变更流程
  -> 变更成功后语音回读，IM 记录影响范围和撤销入口
```

### 7.3 基本概念与信息结构

| 概念 | 定义 |
| --- | --- |
| 单次日程 | 只发生一次的事项，例如“明天 10 点客户拜访” |
| 点状提醒 | 只需要触发时间、不占用时间段的提醒，例如“14:00 取快递” |
| 周期事项 | 按每天、每周或每月重复的事项 |
| 周期实例 | 周期事项在某个日期生成的一次事项 |
| 临时便签 | 用户主动说出的无时间小事，保留 24 小时 |
| 提醒 | 系统在触发事件发生时发送的语音和 IM 通知 |
| 弱提醒 | 日程开始前 15 分钟的轻提示，不要求用户处理 |
| 跳过 | 只取消某一次周期实例，不改变周期规则 |
| 暂停 | 在明确日期前停止周期提醒，到期后恢复 |
| 终止 | 停止周期事项后续提醒，属于高风险操作 |
| 操作回执 | 创建、提醒处理、变更、撤销后的 IM 记录 |
| 对话上下文 | 当前提醒、候选日程和最近一次操作，用来理解“这个”“刚才那条” |

每条日程至少包含：标题、发生时间、结束时间或持续时长、地点、备注、周期规则、提醒时间、状态。

每条操作回执至少包含：用户原话、操作对象、变更前内容、变更后内容、影响范围、操作结果、撤销截止时间。

## 8. 原型 / Demo

原型必须展示语音主流程，而不是只展示日历页面：

1. 语音创建单次日程；
2. 缺少时间或时长时追问；
3. 创建前冲突提示；
4. 语音查询摘要和 IM 分页列表；
5. 事件触发后的语音提醒和 IM 详情；
6. “知道了 / 十分钟后提醒我”的即时处理；
7. 修改本次周期实例；
8. 跳过、删除、确认和撤销。

配套原型文档：[语音日程提醒工具-产品原型设计文档](https://github.com/1024XEngineer/XE6-15/pull/55)。

## 9. 验收标准

1. 用户说“明天上午十点客户拜访，A 座 1503，大概两个小时”时，系统创建 10:00-12:00 的日程，语音回读标题和时间，IM 展示地点和时长。
2. 用户只说“明天提醒我给王姐回电话”时，系统追问具体时间；用户补充时间后再创建。
3. 用户创建占用时间段的日程但缺少结束时间或时长时，系统追问时长后再检查冲突。
4. 已有 10:00-11:00 日程时，新增 11:00-12:00 不算冲突；新增 10:30-11:30 算冲突。
5. 发现冲突后，用户拒绝创建时，系统不留下新日程、新提醒或创建回执；用户确认创建时，IM 标出冲突。
6. 用户能创建每天、每周、每月三类周期事项。
7. 用户说“帮我记一下车停在 B2 蓝区”时，系统生成 24 小时临时便签；24 小时后查询不到该便签。
8. 用户能查询今天、某一天、某一周、某一月、今年、下一条和关键词相关日程；语音播摘要，IM 每页最多 20 条。
9. 主提醒时间到达时，语音和 IM 同时发送；语音默认只播标题和时间，IM 展示完整内容。
10. 有明确开始时间的日程，可以在开始前 15 分钟触发弱提醒；弱提醒不替代主提醒。
11. 用户说“知道了”时，系统关闭当前提醒，底层日程不变。
12. 用户说“十分钟后提醒我”时，系统创建一次稍后提醒，底层日程不变。
13. 同一提醒实例第 3 次推迟时，系统明确告知这是最后一次稍后提醒；再次要求推迟时，系统不再创建新的稍后提醒。
14. 多条提醒同时到达，用户只说“知道了”时，系统先让用户选择目标，不默认关闭任何一条。
15. 用户说“把今天的日报改到晚上七点”时，系统只修改今天这个周期实例，并提示明天仍按原时间提醒。
16. 周期事项范围不清时，系统必须让用户选择“仅本次 / 本次及以后 / 整个系列”。
17. 用户说“这周周报跳过一次”时，系统只跳过本周实例；再次说同一句时，只回复“本次已经跳过”。
18. 用户对单次事项说“跳过”时，系统说明这等同取消，并在确认后执行。
19. 删除、终止和范围不明确的周期操作，不会未经确认直接执行。
20. 删除或变更完成后的 10 分钟内，用户说“撤销刚才操作”，系统恢复最近一次变更前的状态，并发送语音反馈和 IM 回执。
21. 系统不做全天录音、被动捕获、跨天长期记忆和自动移动其他日程。
