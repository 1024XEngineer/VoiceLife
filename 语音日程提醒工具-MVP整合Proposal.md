# 语音日程提醒工具 MVP 整合 Proposal

这份文档把最近三个 Proposal 合成一个 MVP 方案：用户先用语音创建和查询日程，到点后收到语音与 IM 提醒，再用语音或受控 IM 回复处理提醒；需要改动原日程时，进入更谨慎的变更流程。

请评审这一个判断：本期主线是否收敛为“语音创建/查询 + 提醒响应 + 已有日程变更”，并确认哪些能力必须进入 MVP，哪些留到后续。

## 来源

- P1：PR #58，语音创建与查询。
- P2：PR #56，提醒触发与即时响应。
- P3：PR #57，已有日程变更与周期控制。
- 父级提案：Issue #53，语音日程提醒工具 MVP。

## 一句话结论

MVP 不做一个完整日历 App，也不做个人记忆库。它只解决一个连续场景：用户不方便打开手机时，能靠一句话把日程记进去、被准时提醒、临时改动时不被系统误改。

## 目标用户

目标用户是有零散事项和周期提醒需求的人，典型场景是通勤、开会前后、做饭、走路、带娃、路演准备这些不方便手动点 App 的时刻。

他们不是想要一个更复杂的日历页面，而是想少做几步：

- 想到一件事时，直接说出来；
- 到时间时，能被提醒到；
- 当天安排变了，能快速改一次，不影响以后；
- 系统不确定时要问清楚，不能替用户猜。

## 当前做法的问题

| 现在怎么做 | 问题 |
|---|---|
| 打开日历 App 手动新增 | 要解锁、找入口、填多个字段，临时想到的事容易被打断 |
| 用系统语音助手新增提醒 | 能创建，但冲突、周期范围和事后回看都不够清楚 |
| 把事项发给自己或写备忘录 | 能记住内容，但到点不一定提醒，也不容易按日期查 |
| 闹钟响了直接按掉 | 当时处理了提醒，忙完可能忘了提醒的内容 |
| 删除后重新创建 | 临时改期成本高，还容易丢地点、备注和周期规则 |

## 本期范围

### P1：语音创建与查询

用户可以通过语音创建单次日程和简单周期日程，也可以查询今天、某一天、某一周、某一月或今年的待办。

本期创建规则：

- 单次日程至少要有标题和发生时间。
- 会议、拜访、课程这类会占用时间段的日程，如果缺少结束时间或持续时长，系统需要追问。
- 吃药、取快递、打卡这类点状提醒，可以只保留触发时间。
- 简单周期只支持每天、每周、每月这类用户容易说清楚的表达。
- 冲突检查必须发生在创建前。发现冲突时先问用户是否仍要创建，用户拒绝时不留下日程、提醒或回执。

查询规则：

- 用户可以问“我今天有什么事”“下周有什么安排”“7 月 25 日要做什么”“今年还有哪些重要事项”。
- 语音只播摘要：优先播最近几条和总数，避免长列表把用户淹没。
- 完整列表通过 IM 发给用户，方便回看。

### P2：提醒触发与即时响应

P2 只处理“提醒本身”，不修改底层日程。

提醒触发时：

- 语音和 IM 两个通道同时发送，互不阻塞。
- 语音默认只播标题和时间，例如“提醒：14:30 客户拜访”。
- 地点、备注和完整内容放进 IM 回执。
- 用户说“再详细说说”时，语音再补充地点和备注。

用户收到提醒后的合法响应只有两类：

- 关闭提醒：“知道了”“不用再响了”“关闭这条提醒”。
- 稍后提醒：“十分钟后提醒我”“稍后提醒”。

边界规则：

- 用户没有说清推迟多久时，系统追问“多久后再提醒？”；如果用户提前设置了默认推迟时长，才直接使用默认值。
- IM 可以作为受控回复入口，但只作用于当前回执绑定的提醒。
- 多条提醒同时到达时，系统按顺序播报。用户只说“知道了”但目标不清时，系统列出标题让用户选。
- 用户不回应时，不反复语音打扰；IM 记录保留。
- 同一提醒最多连续推迟 3 次，第 3 次后不再自动播报。
- “已处理”“忽略本次”不作为 P2 命令，因为它们听起来像在改事项状态或跳过周期实例。

### P3：已有日程变更与周期控制

P3 处理会改变已有日程或周期规则的操作。

本期支持：

- 修改单次日程的时间、标题、地点或备注。
- 修改周期事项的本次、本次及以后，或整个系列。
- 跳过某一次周期事项，并说明下一次仍按原周期继续。
- 对单次事项说“跳过”时，系统必须解释“这等同于取消这条日程”，确认后才执行。
- 暂停周期事项到一个明确日期，到期后恢复。
- 终止周期事项的后续提醒。
- 删除单次日程或整个周期系列。
- 修改、跳过、暂停、终止、删除后，都要有语音反馈和 IM 回执。
- 用户可以在撤销窗口内恢复刚才的操作；撤销窗口时长留给交互设计确定。

安全规则：

- 目标不明确时不执行，先问用户选哪一条。
- 周期范围不明确时不执行，先问“只改这一次、从这次以后，还是整个系列”。
- 删除、终止、跳过单次事项这类高风险操作必须确认。
- 修改后发生时间冲突时，系统只提示冲突并询问是否继续，不自动移动其他日程。
- 重复跳过同一个周期实例时，只回复“本次已经跳过”，不重复生成变更。

## 明确不做

本期不做这些事：

- 完整 RRULE、复杂周期、法定节假日、农历、跨时区。
- 第三方日历同步、共享日历、修改别人的日程。
- 面向用户的完整日历 GUI。
- 全天候录音、被动捕获、位置或传感器自动触发。
- 外部信号监控，例如“某人没回消息就提醒我”。
- 批量危险操作，例如“清空这个月所有安排”。
- 密码、取件码等敏感内容的长期个人记忆库。停车位这类随手记录可以后续评审，但不进入 MVP 主流程。
- 数据库字段、接口和技术实现方案。

## 关键决策

| 决策 | 选择 | 理由 |
|---|---|---|
| 入口形态 | 语音为主，IM 做回执和受控回复 | 语音适合当下操作，IM 适合事后核对 |
| 查询输出 | 语音播摘要，IM 给完整列表 | 今天、一周、一个月的列表可能很长，全部读出来不好用 |
| 创建前冲突处理 | 先检查，用户确认后再创建 | 避免用户拒绝后还留下脏日程或脏回执 |
| 提醒响应 | P2 只保留关闭和稍后 | “已处理”“忽略本次”容易越界到日程完成或周期跳过 |
| 周期变更 | 明确本次、本次及以后、整个系列 | 用户最怕只想改一次，结果以后全变了 |
| 误操作处理 | 高风险先确认，成功后给撤销 | 既保留语音效率，也给删错、改错留退路 |

## 用户场景

### 场景 1：路上创建客户拜访

用户在路上想起明天要去客户现场，说：“明天上午十点客户拜访，A 座 1503，大概两个小时。”

系统先检查 10:00 到 12:00 是否冲突。如果没有冲突，语音回复“已创建，明天 10 点客户拜访”，IM 留下完整回执。若已有 10:30 的会议，系统先问“10 点到 12 点和项目会冲突，还要创建吗？”用户确认后才创建。

### 场景 2：查询今天和本周安排

用户问：“我今天还有什么事？”

系统语音回复：“今天还有 3 件，最近一件是 14:30 客户拜访，完整列表已发到 IM。”用户问“这周还有哪些提醒”时同理，语音给摘要，IM 给完整列表。

### 场景 3：提醒响起后稍后处理

14:30，系统播报“提醒：客户拜访”。用户正在开会，说：“十分钟后提醒我。”

系统回复“已推迟到 14:40”，IM 回执更新。日程本身仍然是明天上午十点客户拜访，没有被改时间。

### 场景 4：只改今天的周期事项

用户每天 17:30 写日报。今天路演拖延，他说：“把今天的日报推迟到晚上七点。”

系统只修改今天这个周期实例，并回复“已把今天的日报改到 19:00，明天仍然是 17:30”。IM 回执写清楚影响范围是“仅本次”。

### 场景 5：跳过一次，不删除整个周期

用户说：“这周的周报跳过一次。”

系统跳过本周实例，并回复“已跳过本周，下次仍是下周五 17:00”。如果用户又说了一遍，系统回复“本周这次已经跳过”，不再重复变更。

## 验收标准

1. 用户能用语音创建单次日程；缺少必要时间信息时，系统会追问。
2. 对会占用时间段的日程，系统能拿到结束时间或持续时长，再做冲突检查。
3. 发现冲突时，系统在创建或修改前提示用户；用户拒绝时不留下日程、提醒或回执。
4. 用户能查询今天、某一天、某一周、某一月和今年的安排；语音给摘要，IM 给完整列表。
5. 到提醒时间后，语音和 IM 同时触发；任一通道失败不阻塞另一通道。
6. 语音默认只播标题和时间；用户要求详情时再播地点和备注。
7. 用户说“知道了”后，提醒关闭，底层日程不变。
8. 用户说“X 分钟后提醒”后，提醒按用户指定时间再次触发，底层日程不变。
9. 用户没有说清推迟时长时，系统追问；有预设默认值时按默认值处理。
10. 多条提醒同时到达时，用户响应目标不清，系统先让用户选择。
11. 用户说“改到今晚八点”“这周跳过”“以后不用提醒”时，进入 P3，而不是在 P2 里直接处理。
12. 周期事项变更必须让用户知道影响范围是本次、本次及以后，还是整个系列。
13. 跳过周期实例只影响一次；重复跳过不会产生重复变更。
14. 对单次事项说“跳过”时，系统说明等同取消，并在确认后执行。
15. 删除、终止和范围不明确的周期操作不会未经确认直接执行。
16. 修改、跳过、暂停、终止、删除成功后，都有语音反馈和对应 IM 回执。
17. 用户在撤销窗口内撤销时，系统能恢复最近一次变更前的状态。

## 待评审问题

1. 暂停周期事项是否必须进入 MVP，还是只保留跳过一次和终止周期。
2. 撤销窗口定多长，交互上放在 IM 回执里还是另设入口。
3. 对会议、拜访、课程等日程，默认时长是否需要统一，还是一律追问。
4. 停车位这类临时记录是否另开 Proposal，不混入日程提醒 MVP。
